# 供应商寻源系统

> **GitHub 仓库**：https://github.com/xiexieajin/xieajin （分支：`main`）
>
> 所有核心源码均可在 GitHub 上直接查看。下文代码引用格式为"文件名 + 行号"，对应 GitHub 链接规则：
> `https://github.com/xiexieajin/xieajin/blob/main/{文件名}#L{行号}`

# 使用工具

本系统是一个 **Flask Web 应用**，并非影刀/Excel/飞书类的自动化流程，而是一套完整的在线系统。业务人员通过浏览器访问，系统在后台调用多个 AI 模型和外部平台完成"需求解析→供应商搜索→自动初筛→邮件沟通"全链路。

## 技术栈一览

| 类别 | 工具/技术 | 版本 | 用途 |
|------|----------|------|------|
| Web 框架 | Flask | 3.0.3 | 路由、模板渲染、请求处理 |
| 数据库 | MySQL | 8.0+ | pymysql 驱动，从 SQLite 迁移而来解决并发写入锁 |
| AI 文本模型 | DeepSeek | deepseek-v4-pro | 需求解析、关键词生成、供应商过滤、初筛语义判断、邮件生成 |
| AI 视觉模型 | 智谱 GLM | glm-4v-flash（免费） | 图片识别转文字 |
| 工商信息 | 天眼查 MCP | mcp.tianyancha.com | 供应商工商信息补全（注册资本/经营状态/风险/司法案件） |
| 供应商平台 | 1688 官方 API | skills-gateway.1688.com | 国内供应商搜索（搜产品方式） |
| 供应商平台 | MadeInChina MCP | mcp.chexb.com | 中国制造网国际站搜索（MCP over SSE） |
| 海关数据 | 顶易 MCP | mcp.topease.net | 海关进出口数据搜索 |
| 网页抓取 | Jina/Firecrawl/curl/requests | — | 抓取需求文本中 URL 的网页内容，多级回退 |
| 邮件收发 | Gmail SMTP/IMAP | — | 发询价邮件 + 自动轮询收件箱 |
| 本地服务器 | waitress | 3.0.0 | 本地开发 WSGI，支持 SSE 流式响应 |
| 云端部署 | gunicorn | 23.0.0 | Railway 部署，1 worker 4 threads，timeout 3600 |

## 模块文件清单

| 文件 | 行数 | 职责 |
|------|------|------|
| [`app.py`](https://github.com/xiexieajin/xieajin/blob/main/app.py) | ~4000 | Flask 主应用，80+ 路由，任务轮询调度，权限控制 |
| [`db.py`](https://github.com/xiexieajin/xieajin/blob/main/db.py) | ~2100 | 数据库连接、18 张表建表、种子数据初始化、需求状态机 |
| [`supplier_search.py`](https://github.com/xiexieajin/xieajin/blob/main/supplier_search.py) | ~2600 | 跨平台供应商搜索核心（4 平台爬虫+去重+预过滤+AI 过滤） |
| [`screening_engine.py`](https://github.com/xiexieajin/xieajin/blob/main/screening_engine.py) | ~1100 | 规则驱动自动初筛引擎（11 否决+5 评分） |
| [`screening_rules.py`](https://github.com/xiexieajin/xieajin/blob/main/screening_rules.py) | ~600 | 初筛规则加载、条件判定（12 种操作符）、模板管理 |
| [`screening_data.py`](https://github.com/xiexieajin/xieajin/blob/main/screening_data.py) | ~700 | 天眼查数据采集与缓存、风险/资质/司法数据供给 |
| [`screening_audit.py`](https://github.com/xiexieajin/xieajin/blob/main/screening_audit.py) | ~350 | 初筛审计日志记录与报告生成 |
| [`ai_helper.py`](https://github.com/xiexieajin/xieajin/blob/main/ai_helper.py) | ~1300 | DeepSeek/智谱统一调用、需求解析、网页抓取、HS 归类 |
| [`email_service.py`](https://github.com/xiexieajin/xieajin/blob/main/email_service.py) | ~940 | Gmail 收发邮件、IMAP 轮询、待认领邮件、过滤规则 |
| [`communication_ai.py`](https://github.com/xiexieajin/xieajin/blob/main/communication_ai.py) | ~690 | AI 生成邮件回复（会话回复/群发/单发） |
| [`model_config.py`](https://github.com/xiexieajin/xieajin/blob/main/model_config.py) | ~170 | AI 配置从数据库加载与热更新 |
| [`scoring.py`](https://github.com/xiexieajin/xieajin/blob/main/scoring.py) | ~200 | 旧版手动初筛评分（已被 screening_engine 取代） |
| [`file_parser.py`](https://github.com/xiexieajin/xieajin/blob/main/file_parser.py) | ~130 | 上传文件解析（PDF/Excel/Word/图片） |
| [`config.py`](https://github.com/xiexieajin/xieajin/blob/main/config.py) | ~140 | 环境变量读取、API 配置检测 |

## 模块调用关系

```
                          ┌─────────────┐
                          │  app.py     │  Flask 路由入口（python app.py）
                          │  80+ 路由   │
                          └──────┬──────┘
            ┌──────────┬─────────┼──────────┬──────────────┐
            ▼          ▼         ▼          ▼              ▼
       ai_helper  supplier_   screening_  email_      communication_
                  search      engine      service         ai
            │          │         │          │              │
            │          │    ┌────┴────┐     │              │
            │          │    ▼     ▼   ▼     │              │
            │          │ screening  screening_  screening_ │
            │          │ _rules    _data      _audit       │
            │          │    │        │                      │
            ▼          ▼    ▼        ▼                      ▼
       model_config（所有 AI 调用都经此读取配置）    call_deepseek（ai_helper）
            │
            ▼
       ai_providers / ai_model_configs / search_platforms（MySQL 表）
```

**核心调用链**（app.py 内的 import 点）：
- `from ai_helper import parse_requirement` → 需求解析（[app.py L1370](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1370)）
- `from ai_helper import classify_hs_code` → HS 编码分类（[app.py L1616](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1616)）
- `from supplier_search import search_suppliers` → 供应商搜索（[app.py L1769](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1769)）
- `from screening_engine import run_screening` → 自动初筛（[app.py L2066](https://github.com/xiexieajin/xieajin/blob/main/app.py#L2066)）
- `from screening_rules import ...` → 规则配置（app.py 多处）
- `from scoring import calculate_score` → 旧版手动初筛（[app.py L1127](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1127)）
- `from file_parser import parse_uploaded_file` → 文件解析（[app.py L1348](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1348)）

# 逻辑说明

## 需求&目的

这是一套给采购/寻源业务部门使用的**供应商寻源系统**。业务痛点是：手工在多个平台找供应商、逐家查工商风险、逐家发邮件询价，效率极低且容易漏判风险。

系统把整个寻源流程自动化为四个阶段：

1. **AI 需求解析**：业务人员输入一段需求描述（可粘贴网页链接、上传文件/图片），AI 自动提取产品名、核心功能、材质、规格等结构化字段，并生成 P0-P3 分级关键词（供后续跨平台搜索）。
2. **跨平台供应商搜索**：系统用生成的关键词，并发调用 1688、MadeInChina、顶易海关数据三个平台搜索供应商，自动去重、剔除贸易商和配件商、用天眼查补全工商信息、再用 AI 过滤一遍，最终入库。
3. **规则驱动自动初筛**：对搜到的每家供应商，按"11 条一票否决 + 5 项评分（满分 100）"做自动初筛，数据来自天眼查 MCP，全过程写审计日志可追溯。
4. **邮件沟通**：对通过初筛的供应商发询价邮件，Gmail 自动轮询收件箱抓回复，AI 辅助生成回复邮件。

业务人员全程在浏览器操作，看到的是实时进度推送和结构化结果，不需要懂任何代码。

## 实现逻辑

### 阶段一：AI 需求解析

**入口路由**：`/ai/parse-requirement-stream`（POST，[app.py L1393](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1393)），采用 SSE（Server-Sent Events）流式推送进度。

**业务流程**：

```
用户输入（文本/文件/图片）
   │
   ├─ 有图片？→ file_parser 解析 → call_zhipu_vision（智谱GLM-4V）识别为文字
   ├─ 有文档？→ file_parser 解析（PDF/Excel/Word/TXT → 纯文本）
   ├─ 文本含URL？→ fetch_urls_from_text 抓取网页内容
   │                    └─ 多级回退：Jina → Firecrawl → curl → requests
   │                       （总内容上限 20000 字，避免 token 爆炸）
   │
   ▼
call_deepseek（场景码 req_parse）
   ├─ 提取结构化字段：product_name / core_functions / material / spec_size /
   │   target_market / required_certs / 各类采购量参数
   ├─ 判断 confirmed（需求信息是否完整）
   │
   ├─ confirmed=false → _generate_questions 生成追问问题，返回用户补充
   └─ confirmed=true  → _generate_summary_and_keywords
                         ├─ 生成 requirement_summary（需求摘要）
                         └─ 生成 P0-P3 关键词（7 组，每组含 cn/en/variants）
                         └─ classify_hs_code 归类 HS 编码（前 4 位）
   │
   ▼
结果暂存内存字典 _tmp_results（SSE 期间无法写 session cookie）
   │
   ▼
跳转 /ai/parse-requirement-result?rid=xxx 取结果渲染确认页
   │
   ▼
用户确认 → /ai/save-requirement 存入 requirements 表（status='需求确认中'）
```

**P0-P3 关键词结构**（[ai_helper.py L1192](https://github.com/xiexieajin/xieajin/blob/main/ai_helper.py#L1192)）：共 7 组，每组包含中文词、英文词、变体词：
- `P0`：核心产品名（1 组）
- `P1_1` / `P1_2`：核心功能相关词（2 组）
- `P2_1` / `P2_2`：材质/规格相关词（2 组）
- `P3_1` / `P3_2`：扩展词（2 组）

> 这些关键词是阶段二跨平台搜索的输入，英文词专供 MadeInChina 国际站和海关数据搜索。

### 阶段二：跨平台供应商搜索

**入口路由**：`/ai/search-suppliers/<req_id>`（POST，[app.py L1687](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1687)）。

由于 Railway 云端会切断 5 分钟以上的长连接，搜索采用**"task_id + 后台线程 + 前端轮询"模式**而非 SSE：POST 瞬间返回 task_id，前端每 3 秒轮询进度，断线刷新也能用历史消息恢复进度。

**后台线程 run_search_thread 流程**（[app.py L1765](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1765)）：

```
1. 读取需求的关键词 keywords（P0-P3 的 JSON）和 product_name
2. 调用 supplier_search.search_suppliers(keywords, product_name, progress_callback, hs_code, cancel_checker)
3. 返回的供应商列表逐个去重（requirement_id + name）后 INSERT INTO suppliers
   └─ dev_stage = '已寻源待初筛'
   └─ user_id 跟着"需求所有者"走（管理员代操作时供应商归用户）
4. recalc_requirement_status → 需求 status 推进到 '寻源中'
```

**supplier_search.search_suppliers 内部流程**（[supplier_search.py L2533](https://github.com/xiexieajin/xieajin/blob/main/supplier_search.py#L2533)）：

```
步骤1：解析 P0-P3 关键词（7 组 × cn/en = 14 个搜索词），构建 search_terms 列表
步骤2：get_search_platforms() 读取启用的平台（ali1688 / madeinchina / topease_customs）
步骤3：_translate_keyword_to_english（DeepSeek 翻译中文关键词→英文，供国际站搜索）
步骤4：ThreadPoolExecutor 并发搜索（max_workers=min(3, 关键词数)）
        └─ 每个关键词内部再并发调各平台爬虫
        │
        ├─ crawl_1688（1688 官方 API）
        │   └─ _crawl_1688_once：HMAC-SHA256 签名，POST skills-gateway.1688.com
        │   └─ 返回：name, product_title, product_link, price, moq, content, hit_keyword
        │   └─ 限流：_wait_1688_rate_limit，全局锁+5秒间隔；429 限流时 5×2^attempt 秒退避
        │
        ├─ crawl_topease_customs（顶易海关数据 MCP）
        │   └─ MCP streamable-http：mcp.topease.net/mcp，tool=search_customs_data
        │   └─ 返回：name, customs_export_count, customs_total_qty, customs_total_amount, product_title
        │   └─ 限流：_wait_topease_rate_limit，5秒间隔 + 串行锁
        │
        └─ crawl_made_in_china_mcp（MadeInChina MCP）
            └─ MCP over SSE：mcp.chexb.com/sse（用系统 curl 绕过 Python OpenSSL 问题）
            └─ tool=search_products，返回英文名，后续翻译成中文
            └─ 限流：_wait_mic_rate_limit，5秒间隔 + 串行锁
        │
步骤5：extract_company_names 提取公司名（有 name 字段直接用，无则正则从 content 提取）
步骤6：_dedup_cross_platform 跨平台去重
        └─ _extract_core_name：去括号→去组织后缀（有限公司/厂等）→去地域前缀→取核心字号
        └─ 核心字号相同视为同一家，保留 source_text 更长者
步骤7：_programmatic_prefilter 程序化预过滤（零 token，省钱）
        ├─ 规则1：business_type=="Manufacturer" 直接保留
        ├─ 规则2：公司名含"贸易/商贸/商行/电子商务"等且无制造证据 → 剔除
        └─ 规则3：含"配件/紧固件/零件/模具"等且无制造证据且描述<50字 → 剔除
步骤8：TianyanchaClient 补全工商信息（ThreadPoolExecutor max_workers=5）
        └─ 匹配策略：精确同名 → 英文名匹配 → 相似度≥0.6+字号校验
        └─ 补全字段：registered_capital, operating_status, legal_person, business_scope,
        │           establish_date, establish_years, phone, email, factory_address
        └─ 未匹配天眼查的剔除（_tyc_not_found）
步骤9：_translate_company_names_batch + _translate_product_names_batch
        └─ MIC 返回英文名/产品名，DeepSeek 翻译成中文（格式"中文名（英文名）"）
步骤10：filter_suppliers_with_ai AI 过滤（DeepSeek 场景码 supplier_filter_v2）
        └─ 分批 BATCH_SIZE=50，ThreadPoolExecutor max_workers=8 并发
        └─ 判断：制造商/贸易商，剔除配件/材料/加工，不确定保留为"疑似制造商"
        └─ 输出：name, supplier_type, main_product, intro, has_cross_border_exp
        └─ 失败重试 1 次，仍失败返回原始数据标记 filter_failed=True
步骤11：按 name 最终保险去重，返回结果列表
```

> **性能设计**（对应用户"减少限流等待"的偏好）：三个平台各自 5 秒限流 + 串行锁避免触发封禁；关键词级 3 并发 + 平台级并发 + 天眼查 5 并发 + AI 过滤 8 并发，把串行等待压到最低；程序化预过滤在 AI 之前先免费剔除明显不合格的，减少 AI 调用量。

### 阶段三：规则驱动自动初筛

**入口路由**：`/ai/auto-screening/<req_id>`（POST，[app.py L1958](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1958)），同样用 task_id+轮询模式。

**前置校验**：评分规则满分总和必须=100（25+15+40+15+5），否则拒绝启动初筛。

**screening_engine.run_screening 流程**（[screening_engine.py L112](https://github.com/xiexieajin/xieajin/blob/main/screening_engine.py#L112)）：

```
步骤1：get_active_rules(user_id, template_name) 加载规则
        └─ 拆分为 veto_rules（否决）/ score_rules（评分）
        └─ 若指定 template_name，用用户保存的模板参数覆盖默认值

步骤2：查询待初筛供应商
        └─ JOIN suppliers + requirements
        └─ 条件：requirement_id=需求 AND dev_stage='已寻源待初筛'
                 AND NOT EXISTS（screenings 表已有记录）
        └─ ORDER BY s.id ASC

步骤3：create_run_id() 生成批次 UUID（关联本次所有审计日志）

步骤4：逐个供应商调用 _screen_one_supplier：
        │
        ├─ 4a. 数据采集 query_supplier_full_data（screening_data.py）
        │   └─ 优先读 suppliers 表缓存的天眼查数据（搜索阶段已存，省 MCP 调用）
        │   └─ 仅查 3 个增量维度：get_risk_overview / get_qualifications / get_judicial_case
        │   └─ 手动添加的供应商（无缓存）走完整天眼查流程
        │   └─ match_status=="error" 跳过该供应商；"not_found" 直接 0 分
        │
        ├─ 4b. 提取标准化字段构建 eval_data（reg_capital_wan / established_years /
        │       has_business_exception / has_serious_violation / is_faithless_person /
        │       has_ip_loss_lawsuit / judicial_case / risk_count ...）
        │
        ├─ 4c. 遍历 veto_rules 执行一票否决检查（命中任一即淘汰，不再评分）
        │   见下方"11 条否决规则"表
        │
        ├─ 4d. 未被否决 → 遍历 score_rules 执行评分
        │   见下方"5 项评分规则"表
        │
        ├─ 4e. 总分对比阈值线（threshold_pass=75 / threshold_manual_review=60）
        │   └─ ≥75 → 通过（dev_stage='已通过初筛'）
        │   └─ 60-74 → 人工确认
        │   └─ <60 → 未通过（dev_stage='未通过初筛'）
        │
        ├─ 4f. _write_screening_result 写入 screenings 表
        │   └─ 含 6 维度得分：score_capital_scale / score_operating_years /
        │       score_product_match / score_contact_complete / score_risk_record / score_export_exp
        │
        └─ 4g. log_task 每个检查步骤写 screening_audit_logs（含 input_data/result_data/evidence）

步骤5：generate_audit_report(run_id) 生成审计报告，推送 done

步骤6：recalc_requirement_status 更新需求状态
```

**11 条一票否决规则**（db.py 种子数据，screening_rule_templates 表）：

| 序 | rule_code | 规则名 | 判定条件 | 数据来源 |
|----|-----------|--------|---------|---------|
| 1 | veto_capital | 注册资本一票否决 | reg_capital_wan < 100（万） | get_company_basic_profile |
| 2 | veto_operating_status | 经营状态一票否决 | operating_status 不在[存续/在业/正常/开业] | get_company_basic_profile |
| 3 | veto_abnormal | 经营异常一票否决 | has_business_exception = true | get_risk_overview |
| 4 | veto_dishonest | 严重违法失信一票否决 | has_serious_violation = true | get_risk_overview |
| 5 | veto_no_contact | 无有效联系方式一票否决 | has_valid_contact = false | get_company_basic_profile |
| 6 | veto_non_manufacturer | 非制造商一票否决 | is_manufacturer = false（公司名关键字+经营范围+AI 三层判断） | get_company_basic_profile + get_qualifications |
| 7 | veto_product_mismatch | 产品不匹配一票否决 | product_match = "mismatch"（AI 判断是配件而非主产品） | — |
| 8 | veto_faithless_person | 失信被执行人一票否决 | is_faithless_person = true | get_risk_overview |
| 9 | veto_ip_lawsuit | 知识产权侵权败诉一票否决 | has_ip_loss_lawsuit = true（司法案件含"知识产权/专利/商标"+"败诉/判决"） | get_judicial_case |
| 10 | veto_platform_infringe | 平台侵权下架一票否决 | has_platform_infringe = true（DuckDuckGo 搜索+AI 分析，**始终标 uncertain 需人工复核**） | 互联网搜索 |
| 11 | veto_capital_unknown | 注册资本未披露 | reg_capital_wan is null（不淘汰，转人工确认） | get_company_basic_profile |

> **否决检查的 4 个核心函数**（screening_engine.py）：
> - [`_check_non_manufacturer`](https://github.com/xiexieajin/xieajin/blob/main/screening_engine.py#L610)（L610）：三层判断——公司名关键字（厂/制造 vs 贸易/商贸）→ 经营范围上下文校验 → call_deepseek AI 综合判断
> - [`_check_product_mismatch`](https://github.com/xiexieajin/xieajin/blob/main/screening_engine.py#L772)（L772）：AI 从需求提取主产品（如"茶色玻璃下翻门电视柜"→"电视柜"），判断供应商产品是否就是主产品本身，配件触发否决
> - [`_check_ip_lawsuit`](https://github.com/xiexieajin/xieajin/blob/main/screening_engine.py#L583)（L583）：从天眼查司法案件文本中匹配"知识产权/专利/商标"且"败诉/判决"
> - [`_check_platform_infringe`](https://github.com/xiexieajin/xieajin/blob/main/screening_engine.py#L545)（L545）：DuckDuckGo 搜索公司名 + AI 分析，但**始终 return False**（互联网来源不自动否决，标 uncertain 待人工复核）

**5 项评分规则**（满分 100 = 25+15+40+15+5）：

| rule_code | 规则名 | 满分 | 评分逻辑 |
|-----------|--------|------|---------|
| score_capital_scale | 注册资本与企业规模 | 25 | ≥1000万满分；100-1000万按比例；<100万 0 分 |
| score_operating_years | 经营状态与成立年限 | 15 | ≥5年满分；2-5年按比例；<2年 0 分 |
| score_product_match | 产品匹配度 | 40 | AI 评分 0-100 换算（基础50+核心功能25+材质25+额外5），数据源=product_title+main_product+intro |
| score_risk_record | 风险记录 | 15 | 0条满分；1条一半；≥2条 0 分 |
| score_export_exp | 出口经验 | 5 | 海关出口次数>0 或出口认证/跨境电商 满分；仅进出口经营范围一半；无 0 分 |

**通过标准**（threshold 类型规则）：
- `threshold_pass`：总分 ≥ 75 → 通过初筛
- `threshold_manual_review`：总分 60-74 → 人工确认
- 总分 < 60 → 未通过

> **规则可配置**：所有规则存在 `screening_rule_templates` 表，用户可在 `/screening/rules` 页面修改参数（阈值/满分/启用开关），修改后存为 `screening_rule_instances`（用户规则实例）。支持保存为命名模板（如"严格模式""宽松模式"）复用。条件判定支持 12 种操作符：lt/lte/gt/gte/eq/neq/in/not_in/contains/not_contains/is_null/is_not_null。

### 阶段四：邮件沟通与 AI 回复

**发邮件**：`/suppliers/<id>/send-email`（[app.py L2895](https://github.com/xiexieajin/xieajin/blob/main/app.py#L2895)）→ `email_service.send_email`
- SMTP_SSL 连接 smtp.gmail.com:465，超时 60 秒，重试 2 次
- 发送成功写 communications 表（direction=outbound, status=sent）
- 附件存 uploads 目录，元信息存 communication_attachments 表

**收邮件**：`email_service.start_polling` 启动 daemon 线程
- IMAP4_SSL 连接 imap.gmail.com:993，搜索 UNSEEN 邮件
- 轮询间隔从 email_config.poll_interval 读取（默认 300 秒=5 分钟）
- 按 Message-ID 去重 → `_should_filter_email` 过滤（查 email_filter_rules 表，剔除 noreply/验证码等）→ `_match_supplier_by_email` 按发件人邮箱匹配 suppliers 表
- 匹配成功 → 写 communications（direction=inbound）
- 匹配失败 → 写 pending_emails（待认领邮件），用户在"待认领邮件"页手动关联供应商后转入 communications

**AI 生成邮件**：`communication_ai` 模块
- `generate_session_reply`（会话回复）：取供应商+需求+最近 20 条沟通记录构建上下文，call_deepseek（场景码 comm_reply, json_mode, max_tokens=32768）
- `generate_bulk_or_single`（群发/单发询价）：取供应商+需求参数+模板构建上下文，call_deepseek（场景码 comm_send）
- 支持多语言生成（中/英）、支持模板变量（{product_name}/{supplier_name}）
- 生成结果存 ai_generation_logs 表；用户点"重新生成"时，上次未采纳的结果作为"错误示例"发给 AI 避免重复问题
- `accept_generation` 标记采纳

**AI 邮件配置**（ai_prompt_settings 表，4 个提示词）：
- `comm_system_prompt`：系统提示词（定义 AI 角色和邮件基本要求）
- `prompt_session_reply`：会话回复默认提示词
- `prompt_bulk_send`：群发询价默认提示词
- `prompt_single_send`：单发询价默认提示词

### 数据模型（18 张表）

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| requirements | 需求 | product_name, core_functions, material, keywords(P0-P3 JSON), hs_code, status |
| suppliers | 供应商 | requirement_id, name, dev_stage, hit_keyword, supplier_type, registered_capital, product_title, customs_export_count, tyc_match_status, business_scope, user_id |
| screenings | 初筛结果 | supplier_id, quality_score, score_capital_scale/operating_years/product_match/risk_record/export_exp, run_id |
| communications | 沟通记录 | supplier_id, channel, content, conclusion, direction |
| users | 用户 | username, password_hash, display_name, role(admin/user), is_active |
| ai_providers | AI 服务商 | provider_code(deepseek/zhipu/tianyancha/ali1688/madeinchina/topease_customs), api_key, base_url |
| ai_model_configs | AI 场景配置 | scene_code, model_name, thinking_enabled, thinking_effort, max_tokens, temperature |
| search_platforms | 搜索平台配置 | provider_id, is_enabled, priority, max_results |
| screening_rule_templates | 初筛规则模板 | rule_code, rule_type(veto/score/threshold), default_condition, default_action, max_score, tyc_commands |
| screening_rule_instances | 用户规则实例 | requirement_id, template_id, template_name, custom_condition, custom_score_cap |
| screening_audit_logs | 初筛审计日志 | run_id, supplier_id, task_code, input_data, result_data, evidence, status |
| email_config | 邮件配置 | gmail_address, app_password, poll_interval, is_enabled |
| pending_emails | 待认领邮件 | from_addr, subject, body_preview, external_id, is_claimed, claimed_supplier_id |
| email_filter_rules | 邮件剔除规则 | field, match_type, match_value, action(skip), is_builtin |
| communication_templates | 沟通模板 | name, subject_template, body_template, scene(inquiry/follow_up) |
| ai_prompt_settings | AI 提示词设置 | setting_key, setting_value |
| ai_generation_logs | AI 生成记录 | scene, supplier_id, generated_subject, generated_body, is_accepted |
| communication_attachments | 邮件附件 | communication_id, original_filename, saved_filename, is_image |

**需求状态机**（requirements.status）：
```
需求确认中 → 寻源中 → 初筛中 → 沟通中 → 已完成
```
`recalc_requirement_status`（db.py）根据供应商的 dev_stage 和沟通记录自动推进需求状态。

**供应商开发阶段**（suppliers.dev_stage）：
```
已寻源待初筛 → 已通过初筛 / 未通过初筛 → 沟通中 → 已合作
```

**AI 场景码**（ai_model_configs.scene_code，共 8 个）：
| 场景码 | 用途 |
|--------|------|
| req_parse | 需求解析 |
| keyword_gen | 关键词生成 |
| auto_screening | 初筛语义判断 |
| vision_ocr | 图片识别（智谱） |
| comm_reply | 邮件会话回复 |
| comm_send | 邮件群发/单发 |
| supplier_filter_v2 | 供应商 AI 过滤 |
| supplier_translate | 关键词/公司名翻译 |

### 跨模块数据流总图

```
┌──────────────┐
│ 业务人员输入  │ 文本/文件/图片
└──────┬───────┘
       ▼
┌──────────────────────────────────────┐
│ 阶段一：AI 需求解析                    │
│ ai_helper.parse_requirement           │
│   智谱GLM-4V识图 + DeepSeek提取字段    │
│   生成 P0-P3 关键词 + HS编码           │
└──────┬───────────────────────────────┘
       ▼ INSERT requirements (status='需求确认中')
┌──────────────────────────────────────┐
│ 阶段二：跨平台供应商搜索               │
│ supplier_search.search_suppliers      │
│   1688 API + MIC MCP + 顶易海关 MCP   │
│   去重→预过滤→天眼查补全→AI过滤        │
└──────┬───────────────────────────────┘
       ▼ INSERT suppliers (dev_stage='已寻源待初筛')
│        recalc → requirements.status='寻源中'
┌──────────────────────────────────────┐
│ 阶段三：规则驱动自动初筛               │
│ screening_engine.run_screening        │
│   天眼查数据 + 11否决 + 5评分(100分)   │
│   ≥75通过 / 60-74人工 / <60淘汰       │
└──────┬───────────────────────────────┘
       ▼ INSERT screenings + screening_audit_logs
│        通过的 suppliers.dev_stage='已通过初筛'
│        recalc → requirements.status='初筛中'
┌──────────────────────────────────────┐
│ 阶段四：邮件沟通                       │
│ email_service 收发 Gmail              │
│ communication_ai 生成回复              │
└──────┬───────────────────────────────┘
       ▼ INSERT communications + ai_generation_logs
│        suppliers.dev_stage='沟通中'
│        recalc → requirements.status='沟通中'
```

## 特别注意

1. **Railway 长连接超时**：云端部署时 Railway 代理会切断 5 分钟+的长连接。搜索和初筛都改用"task_id + 后台线程 + 前端 3 秒轮询"模式规避，进度消息保留在 task_store 的 messages 列表里，刷新页面可恢复。

2. **数据归属跟着需求所有者走**：管理员帮用户在用户的需求上搜索供应商时，搜出的供应商 user_id 是用户的（不是管理员的），确保用户能完整看到自己的数据。

3. **天眼查数据缓存复用**：搜索阶段查完天眼查后直接存 suppliers 表，初筛阶段读库复用，省掉重复 MCP 调用。只有风险/资质/司法 3 个增量维度才实时查。

4. **平台侵权检查不自动否决**：`_check_platform_infringe` 始终 return False，因为互联网来源可信度有限，只标注 uncertain 待人工复核，避免误杀。

5. **旧版 scoring.py 已废弃**：`scoring.py` 的 `calculate_score` 是旧版手动初筛评分（ip_score 40+qual_score 40+basic_score 20），已被 `screening_engine.py` 的 100 分制取代，仅保留在 screening_create/edit 路由供手动初筛用。

6. **评分规则满分必须=100**：初筛启动前会校验启用的评分规则满分总和=100，否则拒绝启动。单条规则保存允许临时≠100，但执行时必须=100。

7. **平台限流策略**：1688/顶易/MIC 各自 5 秒间隔 + 全局锁串行，避免触发封禁；429 限流时指数退避（5×2^attempt 秒）。

# 操作手册

## 使用说明

### 首次部署

1. **安装依赖**：`pip install -r requirements.txt`
2. **配置环境变量**：复制 `.env.example` 为 `.env`，填入真实密钥：
   - MySQL 数据库连接信息
   - DeepSeek API Key（文本理解/搜索/初筛）
   - 智谱 API Key（图片识别）
   - 天眼查 MCP 认证 token
   - 1688 AccessKey
3. **准备 MySQL 数据库**：创建数据库 `sourcing_db`，用户 `sourcing`
4. **启动**：`python app.py`，浏览器打开 `http://localhost:5000`
5. 首次启动自动建表 + 初始化种子数据（16 条规则 + 2 条阈值线 + AI 配置 + 4 个提示词）

### 云端部署（Railway）

- `Procfile` 配置：`gunicorn app:app --bind [::]:$PORT --timeout 3600 --workers 1 --threads 4`
- 在 Railway 后台配置环境变量（同 .env）
- 单 worker 4 threads，timeout 3600 秒（适应长时间搜索/初筛）

### 日常使用流程

1. **管理员配置**（首次）：
   - 管理中心 → 模型与平台管理：配置 DeepSeek/智谱/天眼查/1688 的 API Key
   - 管理中心 → 搜索平台：启用/禁用平台、设优先级、设最大结果数
   - 管理中心 → 邮件配置：填 Gmail 地址和应用专用密码，启用轮询
   - 筛选规则配置：按需调整否决阈值和评分满分（总分必须=100）

2. **业务操作**：
   - AI 解析需求 → 输入需求描述（可粘链接/传文件/传图片）→ 确认解析结果 → 保存需求
   - AI 搜索供应商 → 选择需求 → 启动搜索 → 等待进度完成 → 查看供应商列表
   - AI 自动初筛 → 选择需求 → 预览规则 → 启动初筛 → 等待完成 → 查看初筛结果/审计详情
   - 邮件沟通 → 对通过初筛的供应商发询价邮件 → Gmail 自动收回复 → 待认领邮件手动关联 → AI 辅助生成回复

## 注意事项

- **DeepSeek API Key 必须先配置**，否则需求解析和搜索都无法使用
- **智谱 API Key** 仅图片识别需要，不传图片可不配
- **Gmail 应用专用密码**不是登录密码，需在 Google 账户开启两步验证后生成
- **搜索和初筛是异步任务**，提交后不要关闭浏览器（虽然刷新可恢复进度，但建议等完成）
- **同一需求同一时间只能运行一个搜索/初筛任务**，系统会自动拦截重复提交
- **初筛审计日志永久保留**，可通过 run_id 追溯每家供应商每个步骤的输入输出和证据

# 附录：如何自己做出这个自动化功能

## 前置准备

### 环境与资源

| 资源 | 获取方式 | 用途 |
|------|---------|------|
| Python 3.10+ | python.org | 运行环境 |
| MySQL 8.0+ | mysql.com | 数据库 |
| DeepSeek API Key | platform.deepseek.com | 文本 AI（需求解析/搜索/初筛/邮件） |
| 智谱 API Key | open.bigmodel.cn | 图片识别（GLM-4V-Flash 免费） |
| 天眼查 MCP token | mcp.tianyancha.com | 工商/风险/司法数据 |
| 1688 AccessKey | 1688 开放平台 | 供应商搜索 |
| Gmail 应用专用密码 | Google 账户 | 邮件收发 |
| 顶易 MCP | mcp.topease.net | 海关数据（可选） |
| MadeInChina MCP | mcp.chexb.com | 国际站搜索（可选） |

### 项目结构搭建

```
供应商寻源系统/
├── app.py              # Flask 主应用
├── db.py               # 数据库层
├── config.py           # 配置读取
├── model_config.py     # AI 配置热更新
├── ai_helper.py        # AI 调用统一封装
├── supplier_search.py  # 供应商搜索
├── screening_engine.py # 初筛引擎
├── screening_rules.py  # 规则管理
├── screening_data.py   # 天眼查数据采集
├── screening_audit.py  # 审计日志
├── scoring.py          # 旧版评分（保留）
├── email_service.py    # 邮件收发
├── communication_ai.py # AI 邮件生成
├── file_parser.py      # 文件解析
├── templates/          # Jinja2 模板（36 个页面）
├── static/             # 静态资源
├── uploads/            # 上传文件
├── requirements.txt
├── Procfile            # Railway 部署
├── .env                # 密钥（不提交 Git）
└── .env.example
```

## 分模块实现要点

### 1. 数据库层（db.py）

- 启动时 `init_db()` 创建 18 张表 + `_seed_screening_rules` 初始化 18 条规则 + `_seed_ai_configs` 初始化 AI 配置
- 用 `_add_column_if_not_exists` / `_drop_column_if_exists` 做平滑字段迁移，兼容旧数据库
- `recalc_requirement_status` 是状态机核心：根据供应商 dev_stage 和沟通记录自动推进需求 status
- `now_str()` 统一时间格式

### 2. AI 调用封装（ai_helper.py + model_config.py）

- `model_config.load_model_configs_from_db()`：启动时从 ai_providers/ai_model_configs/search_platforms 三张表加载到内存，threading.Lock 保护
- `get_model_config(scene_code)`：返回该场景的模型名/思考强度/温度/超时等配置
- `call_deepseek(messages, scene_code, ...)`：统一 DeepSeek 调用入口，参数优先级=调用点传入>场景配置>默认值；支持 thinking 模式（reasoning_effort: low/medium/high/max）、json_mode、自定义 max_tokens
- `call_zhipu_vision(image_base64, prompt)`：智谱 GLM-4V 图片识别
- 网页抓取 `fetch_url_content`：Jina（免费）→ Firecrawl（需 Key）→ curl（绕 TLS 指纹）→ requests，四级回退

### 3. 供应商搜索（supplier_search.py）

- 关键词翻译先行：中文关键词经 DeepSeek 翻译成英文，供国际站和海关搜索
- 三平台并发 + 各自限流：每平台 5 秒间隔 + 全局锁串行，避免封禁
- 跨平台去重靠核心字号：去括号/后缀/地域后比对，保留信息更全的记录
- 程序化预过滤在 AI 之前：零 token 剔除明显贸易商和配件商，省钱
- 天眼查补全后未匹配的剔除：确保每家入库供应商都有工商数据支撑初筛
- AI 过滤分批 50 条 + 8 并发：控制单次 token 量，加速处理

### 4. 初筛引擎（screening_engine.py）

- 规则驱动为主，AI 语义为辅：否决和评分都有明确规则，AI 仅用于非制造商判断和产品匹配度评分
- 天眼查数据缓存复用：搜索阶段已存的工商信息直接读库，只增量查风险/资质/司法
- 全过程审计：每个检查步骤的 input_data/result_data/evidence 都写 screening_audit_logs，run_id 关联
- 状态机自动推进：初筛完调 recalc_requirement_status

### 5. 邮件系统（email_service.py + communication_ai.py）

- 发邮件：SMTP_SSL，失败重试 2 次，成功/失败都写 communications
- 收邮件：daemon 线程 IMAP 轮询，按 Message-ID 去重，过滤规则剔除垃圾邮件，匹配不到供应商的进 pending_emails
- AI 生成：构建上下文（供应商+需求+沟通记录+模板），DeepSeek 生成，未采纳结果作错误示例反馈

## 部署上线

1. 本地开发：`python app.py`（waitress，支持 SSE）
2. 云端部署：Railway + gunicorn，配置环境变量
3. 健康检查：`/health` 端点
4. 日志：print 输出到 stdout（Railway 自动收集）

# 附录二：核心代码清单

> 本系统代码量较大（核心 .py 文件合计 600KB+），以下收录各模块最核心函数的关键实现片段。完整源码见 GitHub 仓库：https://github.com/xiexieajin/xieajin

## A. app.py — 核心路由架构

### 任务轮询模式（搜索/初筛通用）

> 📎 [app.py L1687](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1687)

```python
# app.py L1687 — AI搜索供应商路由
@app.route("/ai/search-suppliers/<int:req_id>", methods=["GET", "POST"])
def ai_search_suppliers(req_id):
    # POST：提交搜索任务（返回task_id，由前端轮询进度）
    import uuid
    task_id = str(uuid.uuid4())
    # 创建任务前在锁内检查，避免同一需求被重复启动多个搜索线程
    with _task_store_lock:
        for existing_task_id, existing_task in task_store.items():
            if (existing_task.get("task_type") == "search"
                    and existing_task.get("req_id") == req_id
                    and existing_task.get("status") in ("running", "cancelling")):
                return jsonify({"task_id": existing_task_id, "status": "running"})
        task_store[task_id] = {
            "messages": [], "status": "running", "result": None,
            "req_id": req_id, "task_type": "search", "cancel_requested": False,
        }
    # 启动后台搜索线程
    thread = threading.Thread(target=run_search_thread)
    thread.start()
    return jsonify({"task_id": task_id, "status": "started"})
```

### 后台搜索线程（调用 search_suppliers 并入库）

> 📎 [app.py L1765](https://github.com/xiexieajin/xieajin/blob/main/app.py#L1765)

```python
# app.py L1765 — run_search_thread
def run_search_thread():
    conn = db.get_db()
    cur = conn.cursor()
    try:
        from supplier_search import search_suppliers
        keywords = requirement["keywords"] or requirement["product_name"]
        hs_code = requirement.get("hs_code", "") or ""
        suppliers = search_suppliers(
            keywords, requirement["product_name"], progress_callback,
            hs_code, cancel_checker=is_search_cancelled,
        )
        saved_count = 0
        for s in suppliers:
            supplier_name = (s.get("name") or "").strip()
            if not supplier_name:
                continue
            # 去重：同需求下同名供应商不重复插入
            cur.execute("SELECT id FROM suppliers WHERE requirement_id=%s AND TRIM(name)=%s LIMIT 1",
                        (req_id, supplier_name))
            if cur.fetchone():
                continue
            cur.execute("""INSERT INTO suppliers
                (requirement_id, name, intro, factory_address, email, phone,
                 main_product, source, dev_stage, hit_keyword, supplier_type, ...)
                VALUES (%s, %s, %s, ..., '已寻源待初筛', ...)""", (...))
            saved_count += 1
        recalc_requirement_status(cur, req_id)  # 推进需求状态到'寻源中'
        conn.commit()
```

## B. ai_helper.py — DeepSeek 统一调用

> 📎 [ai_helper.py L715](https://github.com/xiexieajin/xieajin/blob/main/ai_helper.py#L715)

```python
# ai_helper.py L715 — call_deepseek
def call_deepseek(messages, scene_code, temperature=None, json_mode=False, max_tokens=None, effort=None):
    """按场景码调用 DeepSeek，参数优先级：调用点 > 场景配置 > 默认值"""
    cfg = get_model_config(scene_code)        # 从数据库读场景配置
    provider = get_provider("deepseek")        # 读服务商 base_url + api_key
    # 参数优先级处理
    actual_effort = effort or (cfg.get("thinking_effort") if cfg.get("thinking_enabled") else None)
    actual_temp = temperature if temperature is not None else cfg.get("temperature")
    actual_max_tokens = max_tokens or cfg.get("max_tokens")
    # 构建请求体
    body = {"model": cfg["model_name"], "messages": messages, "max_tokens": actual_max_tokens}
    if cfg.get("thinking_enabled") and actual_effort:
        body["thinking"] = {"type": "enabled"}
        body["reasoning_effort"] = actual_effort
    if json_mode:
        body["response_format"] = {"type": "json_object"}
    if not cfg.get("thinking_enabled") and actual_temp is not None:
        body["temperature"] = actual_temp
    # POST 请求
    resp = requests.post(f"{provider['base_url']}/chat/completions",
                         headers={"Authorization": f"Bearer {provider['api_key']}"},
                         json=body, timeout=cfg.get("timeout_seconds", 120))
    return resp.json()["choices"][0]["message"]["content"]
```

## C. supplier_search.py — 跨平台搜索主流程

> 📎 [supplier_search.py L2533](https://github.com/xiexieajin/xieajin/blob/main/supplier_search.py#L2533)

```python
# supplier_search.py L2533 — search_suppliers 主入口
def search_suppliers(keywords_json, product_name, progress_callback=None, hs_code="", cancel_checker=None):
    # 1. 解析 P0-P3 关键词（7组×cn/en）
    search_terms = _build_search_terms(keywords_json)
    # 2. 读取启用平台
    enabled_codes = [p["provider_code"] for p in get_search_platforms()]
    # 3. 翻译关键词为英文（供国际站/海关搜索）
    # 4. 并发搜索各平台
    all_results = []
    with ThreadPoolExecutor(max_workers=min(3, len(search_terms))) as pool:
        futures = {pool.submit(_search_one_term, term, enabled_codes, hs_code): term for term in search_terms}
        for fut in as_completed(futures):
            all_results.extend(fut.result())
    # 5. 提取公司名 + 跨平台去重
    companies = extract_company_names(all_results)
    companies = _dedup_cross_platform(companies)
    # 6. 程序化预过滤（零token剔除贸易商/配件商）
    companies = _programmatic_prefilter(companies, product_name)
    # 7. 天眼查补全工商信息（5并发）
    companies = _enrich_with_tianyancha(companies, progress_callback)
    # 8. 翻译 MIC 英文名/产品名为中文
    companies = _translate_company_names_batch(companies)
    companies = _translate_product_names_batch(companies)
    # 9. AI 过滤（分批50，8并发）
    companies = filter_suppliers_with_ai(companies, product_name, keywords_text, progress_callback, cancel_checker)
    # 10. 最终去重返回
    return _final_dedup(companies)
```

### 程序化预过滤（零 token）

> 📎 [supplier_search.py L2185](https://github.com/xiexieajin/xieajin/blob/main/supplier_search.py#L2185)

```python
# supplier_search.py L2185 — _programmatic_prefilter
def _programmatic_prefilter(companies, product_name):
    """零token预过滤：剔除明显贸易商和配件商，省AI调用费"""
    trade_keywords = ["贸易", "商贸", "商行", "电子商务", "经营部", "零售", "供应链", "进出口"]
    manufacture_evidence = ["OEM", "ODM", "工厂", "制造", "生产", "Manufacturer"]
    accessory_keywords = ["配件", "紧固件", "零件", "部件", "模具", "夹具", "治具", "原材料"]
    kept = []
    for c in companies:
        source_text = (c.get("source_text") or "")
        # 规则1：明确是 Manufacturer 直接保留
        if c.get("business_type") == "Manufacturer":
            kept.append(c); continue
        # 规则2：公司名含贸易词且无制造证据 → 剔除
        if any(k in c["name"] for k in trade_keywords) and not any(k in source_text for k in manufacture_evidence):
            continue
        # 规则3：含配件词且无制造证据且描述短 → 剔除
        if any(k in source_text for k in accessory_keywords) and not any(k in source_text for k in manufacture_evidence) and len(source_text) < 50:
            continue
        kept.append(c)
    return kept
```

## D. screening_engine.py — 初筛引擎

> 📎 [screening_engine.py L112](https://github.com/xiexieajin/xieajin/blob/main/screening_engine.py#L112)

```python
# screening_engine.py L112 — run_screening 主入口
def run_screening(requirement_id, user_id, progress_queue=None, template_name=None):
    # 1. 加载规则
    all_rules = get_active_rules(user_id, template_name)
    veto_rules = [r for r in all_rules if r["rule_type"] == "veto"]
    score_rules = [r for r in all_rules if r["rule_type"] == "score"]
    # 2. 查待初筛供应商
    suppliers = _query_pending_suppliers(requirement_id)
    # 3. 生成批次ID
    run_id = create_run_id()
    # 4. 逐个筛选
    for idx, supplier in enumerate(suppliers):
        _screen_one_supplier(supplier, veto_rules, score_rules, run_id,
                             owner_user_id, progress_queue, idx, template_name)
    # 5. 生成审计报告
    report = generate_audit_report(run_id, user_id=owner_user_id)
    # 6. 更新需求状态
    recalc_requirement_status(cur, requirement_id)
    return report
```

### 单供应商筛选（否决+评分）

> 📎 [screening_engine.py L248](https://github.com/xiexieajin/xieajin/blob/main/screening_engine.py#L248)

```python
# screening_engine.py L248 — _screen_one_supplier
def _screen_one_supplier(supplier, veto_rules, score_rules, run_id, user_id, progress_queue, idx=0, template_name=None):
    # a. 天眼查数据采集（优先读库缓存，增量查风险/资质/司法）
    tyc_data = query_supplier_full_data(supplier, run_id, user_id)
    if tyc_data.get("match_status") == "error":
        return  # 跳过
    # b. 构建标准化评估数据
    eval_data = _build_eval_data(supplier, tyc_data)
    # c. 一票否决检查
    vetoed = False
    for rule in veto_rules:
        if not rule.get("is_enabled"):
            continue
        condition = parse_condition(rule["default_condition"])
        if evaluate_condition(condition, eval_data):
            vetoed = True
            _write_screening_result(supplier, run_id, user_id, "veto", reason, ...)
            break
    if vetoed:
        return
    # d. 评分
    total_score = 0
    for rule in score_rules:
        if not rule.get("is_enabled"):
            continue
        score = _evaluate_score_rule(rule, eval_data, supplier, tyc_data, run_id, ...)
        total_score += score
    # e. 对比阈值线
    if total_score >= 75:
        conclusion = "pass"
    elif total_score >= 60:
        conclusion = "manual_review"
    else:
        conclusion = "fail"
    # f. 写结果
    _write_screening_result(supplier, run_id, user_id, conclusion, reason, total_score, ...)
```

## E. email_service.py — Gmail 收发

> 📎 [email_service.py L130](https://github.com/xiexieajin/xieajin/blob/main/email_service.py#L130)

```python
# email_service.py L130 — send_email
def send_email(to_addr, subject, body, supplier_id=None, user_id=1, attachments=None):
    cfg = get_email_config()
    # SMTP_SSL 连接 Gmail
    with smtplib.SMTP_SSL("smtp.gmail.com", 465, timeout=60) as smtp:
        smtp.login(cfg["gmail_address"], cfg["app_password"])
        msg = MIMEMultipart()
        msg["From"] = cfg["sender_name"]
        msg["To"] = to_addr
        msg["Subject"] = subject
        msg.attach(MIMEText(body, "html"))
        # 添加附件...
        smtp.sendmail(cfg["gmail_address"], [to_addr], msg.as_string())
    # 记录到 communications 表
    _save_communication(supplier_id, "邮件", "outbound", subject, body, "sent", user_id)
```

> 📎 [email_service.py L460](https://github.com/xiexieajin/xieajin/blob/main/email_service.py#L460)

```python
# email_service.py L460 — poll_inbox_once
def poll_inbox_once():
    """IMAP 轮询收件箱，抓取未读邮件"""
    with imaplib.IMAP4_SSL("imap.gmail.com", 993) as imap:
        imap.login(addr, pwd)
        imap.select("INBOX")
        _, msg_nums = imap.search(None, "UNSEEN")
        for num in msg_nums[0].split():
            msg = email.message_from_bytes(imap.fetch(num, "(RFC822)")[0][1])
            msg_id = msg["Message-ID"]
            if _is_email_already_saved(msg_id):   # 去重
                continue
            if _should_filter_email(from_addr, from_name, subject, body):  # 过滤垃圾
                continue
            supplier = _match_supplier_by_email(from_addr)  # 匹配供应商
            if supplier:
                _save_communication(supplier["id"], "邮件", "inbound", ...)  # 命中
            else:
                _save_pending_email(from_addr, from_name, subject, body_preview, msg_id, ...)  # 待认领
```

## F. communication_ai.py — AI 邮件生成

> 📎 [communication_ai.py L157](https://github.com/xiexieajin/xieajin/blob/main/communication_ai.py#L157)

```python
# communication_ai.py L157 — generate_session_reply
def generate_session_reply(supplier_id, user_prompt="", prev_log_id=None, languages=None, template_id=None):
    # 1. 查供应商+需求
    supplier = _get_supplier_with_requirement(supplier_id)
    # 2. 查最近20条沟通记录
    records = _get_communications(supplier_id, limit=20)
    # 3. 构建上下文
    context = _build_session_context(supplier, records)
    # 4. 系统提示词（从 ai_prompt_settings 表读）
    system_prompt = _build_system_prompt(languages)
    # 5. 用户提示词为空时用默认提示词
    if not user_prompt:
        user_prompt = _get_default_prompt("session_reply")
    # 6. 上次生成结果作错误示例（避免重复问题）
    prev_example = _get_prev_example(prev_log_id) if prev_log_id else ""
    # 7. 调 DeepSeek 生成
    messages = _build_messages(system_prompt, user_prompt, context, prev_example)
    result = call_deepseek(messages, scene_code="comm_reply", json_mode=True, max_tokens=32768)
    # 8. 解析并存日志
    parsed = _parse_ai_result(result, languages)
    log_id = _save_generation_log(supplier_id, "session_reply", user_prompt, system_prompt, parsed, ...)
    return {"log_id": log_id, "subject": parsed["subject"], "body": parsed["body"]}
```

## G. 数据库表结构（核心 4 张表）

> 📎 [db.py L253](https://github.com/xiexieajin/xieajin/blob/main/db.py#L253) · [L281](https://github.com/xiexieajin/xieajin/blob/main/db.py#L281) · [L484](https://github.com/xiexieajin/xieajin/blob/main/db.py#L484) · [L527](https://github.com/xiexieajin/xieajin/blob/main/db.py#L527)

```sql
-- db.py L253 — 需求表
CREATE TABLE IF NOT EXISTS requirements (
    id                  INT AUTO_INCREMENT PRIMARY KEY,
    product_name        TEXT NOT NULL,            -- 产品名称（必填）
    product_aliases     VARCHAR(2000) DEFAULT '',  -- 产品别名
    core_functions      VARCHAR(1000) DEFAULT '',  -- 核心功能
    material            VARCHAR(1000) DEFAULT '',  -- 材质
    spec_size           VARCHAR(1000) DEFAULT '',  -- 规格尺寸
    target_market       VARCHAR(1000) DEFAULT '',  -- 目标市场
    required_certs      VARCHAR(1000) DEFAULT '',  -- 需要认证
    requirement_summary TEXT,                      -- AI生成的需求总结
    keywords            TEXT,                      -- P0-P3关键词（JSON）
    status              VARCHAR(50) DEFAULT '需求确认中',
    hs_code             VARCHAR(20) DEFAULT '',    -- HS编码
    created_at          TEXT NOT NULL,
    updated_at          TEXT NOT NULL
);

-- db.py L281 — 供应商表（关键字段）
CREATE TABLE IF NOT EXISTS suppliers (
    id                  INT AUTO_INCREMENT PRIMARY KEY,
    requirement_id      INTEGER NOT NULL,
    name                TEXT NOT NULL,
    dev_stage           VARCHAR(50) DEFAULT '已寻源待初筛',
    hit_keyword         VARCHAR(1000) DEFAULT '',  -- 命中关键词
    supplier_type       VARCHAR(1000) DEFAULT '',  -- 制造商/疑似制造商
    registered_capital  VARCHAR(1000) DEFAULT '',
    operating_status    VARCHAR(50) DEFAULT '存续',
    source              VARCHAR(100) DEFAULT '手动添加',
    product_title       VARCHAR(1000) DEFAULT '',  -- 产品名（业务核对用）
    customs_export_count INT DEFAULT 0,            -- 海关出口次数
    tyc_match_status    VARCHAR(50) DEFAULT '',    -- 天眼查匹配状态
    business_scope      TEXT,                      -- 经营范围
    user_id             INTEGER,
    FOREIGN KEY (requirement_id) REFERENCES requirements (id)
);

-- db.py L484 — 初筛规则模板表
CREATE TABLE IF NOT EXISTS screening_rule_templates (
    id                  INT AUTO_INCREMENT PRIMARY KEY,
    rule_code           VARCHAR(50) NOT NULL UNIQUE,
    rule_name           TEXT NOT NULL,
    rule_type           TEXT NOT NULL,             -- veto / score / threshold
    default_condition   TEXT NOT NULL,             -- 条件JSON
    default_action      TEXT NOT NULL,             -- 动作JSON
    max_score           INTEGER,                   -- 评分满分
    scoring_logic       VARCHAR(1000) DEFAULT '',
    tyc_commands        VARCHAR(1000) DEFAULT '[]',-- 需要的天眼查工具
    is_enabled          INTEGER NOT NULL DEFAULT 1,
    sort_order          INTEGER NOT NULL DEFAULT 0,
    description         TEXT NOT NULL
);

-- db.py L527 — 初筛审计日志表
CREATE TABLE IF NOT EXISTS screening_audit_logs (
    id                  INT AUTO_INCREMENT PRIMARY KEY,
    run_id              TEXT NOT NULL,             -- 批次UUID
    supplier_id         INTEGER NOT NULL,
    task_code           TEXT NOT NULL,             -- 任务编码
    task_name           TEXT NOT NULL,
    input_data          TEXT NOT NULL,             -- 输入快照JSON
    result_data         TEXT NOT NULL,             -- 结果JSON
    evidence            TEXT NOT NULL,             -- 证据来源
    status              VARCHAR(20) DEFAULT 'success', -- success/fail/skip/uncertain
    user_id             INTEGER NOT NULL DEFAULT 1,
    FOREIGN KEY (supplier_id) REFERENCES suppliers (id)
);
```

## H. 文件清单汇总

| 文件 | GitHub 链接 | 用途 |
|------|------------|------|
| app.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/app.py) | Flask 主应用，80+ 路由，任务调度，权限控制 |
| db.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/db.py) | 18 张表建表+迁移，种子数据，需求状态机 |
| config.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/config.py) | 环境变量读取，API 配置检测 |
| model_config.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/model_config.py) | AI 配置数据库加载与热更新 |
| ai_helper.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/ai_helper.py) | DeepSeek/智谱调用，需求解析，网页抓取，HS 归类 |
| supplier_search.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/supplier_search.py) | 4 平台爬虫+去重+预过滤+天眼查+AI 过滤 |
| screening_engine.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/screening_engine.py) | 11 否决+5 评分初筛引擎 |
| screening_rules.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/screening_rules.py) | 规则加载/判定/模板管理 |
| screening_data.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/screening_data.py) | 天眼查数据采集与缓存 |
| screening_audit.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/screening_audit.py) | 审计日志与报告 |
| scoring.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/scoring.py) | 旧版手动初筛评分（已废弃，保留兼容） |
| email_service.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/email_service.py) | Gmail SMTP 发送 + IMAP 轮询接收 |
| communication_ai.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/communication_ai.py) | AI 生成邮件回复（会话/群发/单发） |
| file_parser.py | [查看](https://github.com/xiexieajin/xieajin/blob/main/file_parser.py) | PDF/Excel/Word/图片解析 |
| requirements.txt | [查看](https://github.com/xiexieajin/xieajin/blob/main/requirements.txt) | Python 依赖 |
| Procfile | [查看](https://github.com/xiexieajin/xieajin/blob/main/Procfile) | Railway 部署配置 |
| .env.example | [查看](https://github.com/xiexieajin/xieajin/blob/main/.env.example) | 环境变量模板 |
| templates/ | [查看目录](https://github.com/xiexieajin/xieajin/tree/main/templates) | 36 个 Jinja2 模板页面 |
| static/ | [查看目录](https://github.com/xiexieajin/xieajin/tree/main/static) | CSS/JS 静态资源 |
| uploads/ | — | 上传文件存储（不提交 Git） |

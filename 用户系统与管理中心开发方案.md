# 供应商寻源系统 - 用户系统与管理中心开发方案

## 一、总体架构概览

### 1.1 改造目标

在现有系统基础上增加两层功能：

| 层级 | 功能 | 说明 |
|------|------|------|
| 用户系统 | 登录/注册/权限控制 | 所有用户必须登录才能使用，数据按用户隔离 |
| 管理中心 | 用户管理 + 模型管理 | 仅管理员可见，不占用普通用户的导航空间 |

### 1.2 角色定义

| 角色 | 描述 | 权限 |
|------|------|------|
| admin（管理员） | 系统管理者 | 查看所有数据、进入管理中心、管理用户、管理模型配置 |
| user（普通用户） | 日常使用者 | 只能查看和操作自己的数据（需求、供应商等） |

### 1.3 权限矩阵

| 操作 | admin | user |
|------|-------|------|
| 创建/查看/编辑/删除自己的需求 | ✅ | ✅ |
| 创建/查看/编辑/删除自己的供应商 | ✅ | ✅ |
| AI搜索供应商 | ✅ | ✅ |
| AI自动初筛 | ✅ | ✅ |
| 查看其他用户的数据 | ✅（管理中心内） | ❌ |
| 访问管理中心 | ✅ | ❌（无入口、无法访问） |
| 管理用户（增删改查、重置密码） | ✅ | ❌ |
| 管理模型配置（增删改、切换模型） | ✅ | ❌ |

---

## 二、数据库改造

### 2.1 新增表：users（用户表）

```sql
CREATE TABLE IF NOT EXISTS users (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,   -- 用户ID
    username        TEXT NOT NULL UNIQUE,                -- 登录用户名
    password_hash   TEXT NOT NULL,                       -- bcrypt加密后的密码
    display_name    TEXT NOT NULL,                       -- 显示名称（页面右上角显示用）
    role            TEXT NOT NULL DEFAULT 'user',        -- 角色：admin / user
    is_active       INTEGER NOT NULL DEFAULT 1,          -- 是否启用：1=启用，0=停用
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);
```

### 2.2 新增表：ai_providers（AI供应商/提供商表）

存放所有外部服务商信息。通过 `provider_type` 区分三类：

- `ai_model`：AI大模型供应商（DeepSeek、智谱），有模型配置
- `search_platform`：供应商搜索平台（1688、中国制造网），参与搜索流程
- `data_api`：数据查询API（天眼查），仅管理密钥

```sql
CREATE TABLE IF NOT EXISTS ai_providers (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    provider_name   TEXT NOT NULL UNIQUE,     -- 显示名称，如"DeepSeek"、"1688"、"天眼查"
    provider_code   TEXT NOT NULL UNIQUE,     -- 代码标识，如"deepseek"、"ali1688"、"tianyancha"
    provider_type   TEXT NOT NULL DEFAULT 'ai_model', -- 类型：ai_model / search_platform / data_api
    base_url        TEXT NOT NULL DEFAULT '', -- API接口地址
    api_key         TEXT NOT NULL DEFAULT '', -- API密钥
    is_enabled      INTEGER NOT NULL DEFAULT 1, -- 是否启用
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);
```

### 2.3 新增表：ai_model_configs（模型配置表）

每个AI调用场景一条配置。目前共 **7个场景**（去掉了已废弃的联网搜索）。

```sql
CREATE TABLE IF NOT EXISTS ai_model_configs (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    provider_id     INTEGER NOT NULL,             -- 关联 ai_providers.id
    scene_code      TEXT NOT NULL UNIQUE,         -- 场景代码（代码中写死，不可修改）
    scene_name      TEXT NOT NULL,                -- 场景显示名
    model_name      TEXT NOT NULL,                -- 模型名，如"deepseek-v4-pro"、"glm-4v-flash"
    thinking_enabled INTEGER NOT NULL DEFAULT 0,  -- 是否开启思考模式（仅DeepSeek类模型有效）
    thinking_effort TEXT NOT NULL DEFAULT '',     -- 思考强度：low/medium/high/max
    max_tokens      INTEGER NOT NULL DEFAULT 4096,-- 最大输出token数
    temperature     REAL NOT NULL DEFAULT 0.3,    -- 温度值（思考模式下忽略）
    timeout_seconds INTEGER NOT NULL DEFAULT 120, -- 超时时间（秒）
    extra_params    TEXT NOT NULL DEFAULT '{}',   -- 额外参数（JSON格式，便于扩展）
    sort_order      INTEGER NOT NULL DEFAULT 0,   -- 排序（管理中心展示顺序）
    is_enabled      INTEGER NOT NULL DEFAULT 1,
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL,
    FOREIGN KEY (provider_id) REFERENCES ai_providers(id)
);
```

### 2.4 新增表：search_platforms（搜索平台配置表）

与 `ai_providers` 中 `provider_type=search_platform` 的记录一一对应。管理员在此控制搜索时启用哪些平台。

```sql
CREATE TABLE IF NOT EXISTS search_platforms (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    provider_id     INTEGER NOT NULL UNIQUE,   -- 关联 ai_providers.id
    is_enabled      INTEGER NOT NULL DEFAULT 1, -- 是否参与搜索
    priority        INTEGER NOT NULL DEFAULT 0, -- 搜索优先级（数字越小越先搜）
    max_results     INTEGER NOT NULL DEFAULT 50,-- 该平台单次搜索最大结果数
    extra_config    TEXT NOT NULL DEFAULT '{}', -- 平台特有配置（JSON，如搜索间隔、API限流等）
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL,
    FOREIGN KEY (provider_id) REFERENCES ai_providers(id)
);
```

> 说明：`search_platforms` 表之所以独立出来，是因为搜索平台的配置维度（启用与否、优先级、最大结果数）与AI模型完全不同，混在一张表里会导致大量字段对不上的情况。

### 2.5 改造现有表：加 user_id 字段

在以下四张表上各加一个 `user_id` 字段，实现数据隔离：

| 表名 | 新增字段 | SQL |
|------|---------|-----|
| requirements | user_id INTEGER NOT NULL DEFAULT 1 | `ALTER TABLE requirements ADD COLUMN user_id INTEGER NOT NULL DEFAULT 1` |
| suppliers | user_id INTEGER NOT NULL DEFAULT 1 | `ALTER TABLE suppliers ADD COLUMN user_id INTEGER NOT NULL DEFAULT 1` |
| screenings | user_id INTEGER NOT NULL DEFAULT 1 | `ALTER TABLE screenings ADD COLUMN user_id INTEGER NOT NULL DEFAULT 1` |
| communications | user_id INTEGER NOT NULL DEFAULT 1 | `ALTER TABLE communications ADD COLUMN user_id INTEGER NOT NULL DEFAULT 1` |

> 迁移策略：先ADD COLUMN（给DEFAULT 1确保已有数据不报错），然后根据 `suppliers.requirement_id → requirements.user_id` 回填正确的 user_id。最后创建索引 `CREATE INDEX idx_xxx_user_id ON xxx(user_id)`。

---

## 三、用户系统

### 3.1 认证流程

```
用户访问任何页面
    ↓
检查 session 中是否有 user_id
    ↓ 没有 → 跳转登录页
    ↓ 有
从数据库查询用户信息 → 存入 flask.g.current_user
    ↓
继续处理请求（后续代码通过 g.current_user 获取当前用户信息）
```

### 3.2 需要新增的 Flask 路由

| 路由 | 方法 | 功能 | 说明 |
|------|------|------|------|
| `/login` | GET/POST | 登录页 | GET显示表单，POST验证密码并设置session |
| `/register` | GET/POST | 注册页 | GET显示表单，POST创建用户。默认注册为user角色 |
| `/logout` | GET | 退出登录 | 清除session，跳转登录页 |

### 3.3 登录验证装饰器

在 `app.py` 中新增一个装饰器函数：

```python
def login_required(f):
    """装饰器：要求用户已登录才能访问该路由"""
    from functools import wraps
    @wraps(f)
    def decorated(*args, **kwargs):
        if not session.get("user_id"):
            return redirect(url_for("login", next=request.url))
        g.current_user = query_user(session["user_id"])
        return f(*args, **kwargs)
    return decorated

def admin_required(f):
    """装饰器：要求用户是admin角色才能访问"""
    from functools import wraps
    @wraps(f)
    def decorated(*args, **kwargs):
        if not session.get("user_id"):
            return redirect(url_for("login"))
        g.current_user = query_user(session["user_id"])
        if g.current_user["role"] != "admin":
            flash("无权限访问", "danger")
            return redirect(url_for("index"))
        return f(*args, **kwargs)
    return decorated
```

### 3.4 现有路由改造

给 `app.py` 中所有路由（除了 `/login`、`/register`、`/logout`、`/static`）加上 `@login_required` 装饰器。

### 3.5 数据查询改造（数据隔离）

在 `before_request` 中，获取当前用户的 `user_id`。在所有数据库查询的 WHERE 条件中加上 `user_id = ?` 过滤：

```python
@app.before_request
def before_request():
    g.db = get_db()
    g.user_id = session.get("user_id")
    if g.user_id:
        cursor = g.db.cursor()
        cursor.execute("SELECT * FROM users WHERE id = ?", (g.user_id,))
        g.current_user = cursor.fetchone()
```

然后所有查询改为：

```python
# 改造前
cursor.execute("SELECT * FROM requirements WHERE id = ?", (req_id,))

# 改造后（普通用户只查自己的数据；admin不做user_id过滤）
if g.current_user["role"] == "admin":
    cursor.execute("SELECT * FROM requirements WHERE id = ?", (req_id,))
else:
    cursor.execute("SELECT * FROM requirements WHERE id = ? AND user_id = ?", (req_id, g.user_id))
```

> 建议封装一个 `_add_user_filter(base_sql, params)` 工具函数，统一处理 user_id 过滤逻辑，避免每个SQL都写if-else。

---

## 四、管理中心 - 用户管理

### 4.1 路由设计

| 路由 | 方法 | 功能 |
|------|------|------|
| `/admin` | GET | 管理中心首页（概览仪表盘） |
| `/admin/users` | GET | 用户列表页 |
| `/admin/users/create` | GET/POST | 新增用户（管理员手动创建） |
| `/admin/users/<id>/edit` | GET/POST | 编辑用户信息（角色、启用/停用） |
| `/admin/users/<id>/reset-password` | POST | 重置用户密码 |
| `/admin/users/<id>/toggle-status` | POST | 启用/停用用户 |

### 4.2 用户管理页面功能

- **用户列表**：表格展示（用户名、显示名、角色、状态、创建时间、最后登录时间）
  - 支持按角色筛选（全部/admin/user）
  - 支持按状态筛选（全部/启用/停用）
  - 支持按用户名搜索
- **新增用户**：管理员填写用户名、显示名、密码，选择角色
- **编辑用户**：修改显示名、角色、启用/停用状态
- **重置密码**：管理员可为任意用户设置新密码
- **停用用户**：停用后该用户无法登录（session中检查is_active）

### 4.3 首次初始化管理员账号

系统首次启动时，如果 `users` 表为空，自动创建一个默认管理员账号：

- 用户名：`admin`
- 密码：`admin123`（首次登录强制修改密码——可选功能，视开发时间决定）
- 显示名：`系统管理员`
- 角色：`admin`

---

## 五、管理中心 - 模型管理与搜索平台

这是整个方案中最核心的部分，目标：**管理员通过Web界面管理所有AI模型配置和搜索平台，无需改代码。**

### 5.1 设计思路

当前系统所有AI模型配置都硬编码在 `config.py` 中。改造后全部存到数据库，启动时加载到内存，修改后即时生效。

模型管理中心页面的结构如下：

```
管理中心
├── 用户管理          （第四节）
└── 模型与平台管理    （本节）
    ├── Tab1: AI模型配置    → 管理 ai_providers(ai_model类) + ai_model_configs
    └── Tab2: 搜索平台管理  → 管理 ai_providers(search_platform类) + search_platforms
```

> 天眼查、1688 等 API/MCP 服务虽然存储在 `ai_providers` 中，但它们的 `provider_type` 分别为 `data_api` 和 `search_platform`，不会出现在"AI模型配置"的管理页面中。天眼查的密钥在"搜索平台管理"Tab 下的"API密钥管理"区域维护（不参与模型配置，仅提供密钥的查看/更换）。

### 5.2 系统现有AI模型调用场景（共7个，去掉了联网搜索）

| scene_code | 场景名称 | 所在文件 | 当前模型 | 关键参数 |
|------------|---------|---------|------|---------|
| `req_parse` | 需求解析 | ai_helper.py | deepseek-v4-pro | effort=high, temperature=0.2 |
| `keyword_gen` | 关键词生成 | ai_helper.py | deepseek-v4-pro | effort=high, temperature=0.4 |
| `auto_screening` | 自动初筛 | ai_helper.py | deepseek-v4-pro | effort=max, temperature=0.2 |
| `supplier_translate` | 供应商过滤-翻译 | supplier_search.py | deepseek-v4-pro | effort=high |
| `supplier_filter` | 供应商过滤-第一批 | supplier_search.py | deepseek-v4-pro | effort=high, json_mode |
| `supplier_filter_v2` | 供应商过滤-第二批 | supplier_search.py | deepseek-v4-pro | effort=max, json_mode |
| `vision_ocr` | 图片识别 | ai_helper.py | glm-4v-flash | max_tokens=1024 |

> 已废弃并删除：`web_search`（联网搜索），对应的 `zhipu_web_search()` 函数及相关调用代码需清除，详见5.6节。

### 5.3 管理中心页面布局

#### Tab 1：AI模型配置

##### AI供应商列表（一级）

列出 `provider_type=ai_model` 的供应商：

| 供应商名称 | 代号 | API地址 | 状态 | 场景数 | 操作 |
|-----------|------|--------|------|--------|------|
| DeepSeek | deepseek | https://api.deepseek.com | 启用 | 6 | 编辑/测试/查看场景 |
| 智谱AI | zhipu | https://open.bigmodel.cn/api/paas/v4 | 启用 | 1 | 编辑/测试/查看场景 |

点击"查看场景"进入该供应商的场景配置列表。

##### 场景配置列表（二级）

以 DeepSeek 为例：

| 场景名称 | 当前模型 | 思考模式 | 强度 | Token | 温度 | 超时 | 状态 |
|---------|---------|---------|------|-------|------|------|------|
| 需求解析 | deepseek-v4-pro | 开启 | high | 32768 | 0.2 | 300 | 启用 |
| 关键词生成 | deepseek-v4-pro | 开启 | high | 32768 | 0.4 | 300 | 启用 |
| 自动初筛 | deepseek-v4-pro | 开启 | max | 32768 | 0.2 | 300 | 启用 |
| 供应商过滤-翻译 | deepseek-v4-pro | 开启 | high | 32768 | 0.3 | 300 | 启用 |
| 供应商过滤-第一批 | deepseek-v4-pro | 开启 | high | 32768 | 0.1 | 300 | 启用 |
| 供应商过滤-第二批 | deepseek-v4-pro | 开启 | max | 32768 | 0.2 | 300 | 启用 |

每条可编辑的字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| 模型名称 | 文本框 | 如 deepseek-v4-pro、deepseek-v4-flash |
| 是否开启思考模式 | 开关 | |
| 思考强度 | 下拉(low/medium/high/max) | |
| 最大输出Token | 数字 | |
| 温度 | 滑块(0-1) | |
| 超时时间（秒） | 数字 | |
| 启用/停用 | 开关 | 停用后该场景调用会报错 |

#### Tab 2：搜索平台管理

列出 `provider_type=search_platform` 的服务商，用于控制AI搜索供应商时使用哪些平台：

##### 搜索平台列表

| 平台名称 | 代号 | 状态 | 优先级 | 最大结果数 | API地址 | 操作 |
|---------|------|------|--------|-----------|--------|------|
| 1688 | ali1688 | 启用 | 1 | 50 | https://api.1688.com | 编辑/停用/换Key |
| 中国制造网 | madeinchina | 启用 | 2 | 50 | （MCP地址） | 编辑/停用/换Key |

##### 每条可配置的字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| 是否参与搜索 | 开关 | 关闭后AI搜索不会使用该平台 |
| 优先级 | 数字 | 越小越先搜。例如1688=1、中国制造网=2 |
| 最大结果数 | 数字 | 该平台单次搜索最多返回多少条，默认50 |
| API密钥 | 文本框 | 更换该平台的API Key |
| 额外配置 | JSON | 平台特有参数，如搜索间隔(秒) |

##### 下方还有"API密钥管理"小区域

列出 `provider_type=data_api` 的服务商：

| 服务名称 | 代号 | 类型 | 状态 | 操作 |
|---------|------|------|------|------|
| 天眼查 | tianyancha | 工商信息查询 | 启用 | 编辑/换Key |

> 天眼查只管理密钥，不参与模型配置也不参与搜索平台选择。它就是单纯的数据查询API。

### 5.4 搜索平台配置生效机制

管理员在Tab2修改搜索平台配置后，`supplier_search.py` 的搜索流程会动态变化：

```
管理员在页面勾选了"1688"和"中国制造网"，停用了"联网搜索"
    ↓
数据库 search_platforms 表中：
  - ali1688: is_enabled=1, priority=1
  - madeinchina: is_enabled=1, priority=2
    ↓
AI搜索供应商时，代码读取启用的平台列表，按priority排序：
  → 先搜1688
  → 再搜中国制造网
  → 跳过未启用的平台
```

### 5.5 配置读取与生效机制

```
系统启动时：
  db.init_db() 建表
  → load_model_configs_from_db() 读取所有AI配置到内存
  → 存入全局字典 g_model_configs = {scene_code: {...参数...}}
  → 存入 g_providers = {provider_code: {base_url, api_key, ...}}
  → 存入 g_search_platforms = [{provider_code, priority, max_results, ...}]

运行时调用AI：
  ai_helper.call_deepseek() 从 g_model_configs["req_parse"] 读取参数
  → 从 g_providers["deepseek"] 读取 base_url, api_key

运行时搜索供应商：
  supplier_search.py 从 g_search_platforms 读取启用的平台列表
  → 按 priority 排序，逐个平台搜索

管理员修改配置后：
  → 更新数据库
  → 刷新内存中的字典
  → 立即生效，无需重启
```

> 全局字典读写需要 `threading.Lock` 保护，避免SSE搜索线程正在读配置时被刷新覆盖。

### 5.6 清除联网搜索功能

联网搜索已经废弃不用，需要做以下清理：

**代码层面：**

| 文件 | 操作 |
|------|------|
| `supplier_search.py` | 删除 `zhipu_web_search()` 函数（约第43行开始） |
| `supplier_search.py` | 在搜索流程中删除对 `zhipu_web_search()` 的调用 |
| `supplier_search.py` | 删除对 `ZHIPU_API_KEY`、`ZHIPU_BASE_URL` 的import（如果只用于web_search） |

**配置层面：**

- `config.py` 中智谱 web-search-pro 相关的配置保留（智谱API Key仍用于图片识别）
- `ai_model_configs` 预置数据中不包含 `web_search` 这条记录
- `search_platforms` 预置数据中包含 `ali1688` 和 `madeinchina`（由管理员在页面控制启停）

### 5.7 AI模型管理路由设计

| 路由 | 方法 | 功能 |
|------|------|------|
| `/admin/models` | GET | 模型与平台管理首页（Tab切换） |
| `/admin/models/providers/create` | GET/POST | 新增AI供应商 |
| `/admin/models/providers/<id>/edit` | GET/POST | 编辑AI供应商信息 |
| `/admin/models/providers/<id>/test` | POST | 测试AI供应商连接 |
| `/admin/models/providers/<id>/configs` | GET | 查看该供应商下的场景配置列表 |
| `/admin/models/configs/<id>/edit` | GET/POST | 编辑单个场景的模型配置 |
| `/admin/models/configs/create` | GET/POST | 新增场景配置（后期扩展新场景用） |

### 5.8 搜索平台管理路由设计

| 路由 | 方法 | 功能 |
|------|------|------|
| `/admin/platforms` | GET | 搜索平台列表页（Tab2内容） |
| `/admin/platforms/<id>/edit` | GET/POST | 编辑平台配置（启用、优先级、最大结果数） |
| `/admin/platforms/<id>/apikey` | POST | 更换平台API密钥（含天眼查等data_api类） |

### 5.9 预置数据（首次初始化）

**ai_providers 预置：**

| provider_name | provider_code | provider_type | base_url | api_key |
|--------------|---------------|---------------|----------|---------|
| DeepSeek | deepseek | ai_model | https://api.deepseek.com | （从config.py迁移） |
| 智谱AI | zhipu | ai_model | https://open.bigmodel.cn/api/paas/v4 | （从config.py迁移） |
| 1688 | ali1688 | search_platform | https://api.1688.com | （从config.py迁移） |
| 中国制造网 | madeinchina | search_platform | （MCP地址） | （如有） |
| 天眼查 | tianyancha | data_api | https://mcp.tianyancha.com/v1 | （从config.py迁移） |

**ai_model_configs 预置（7条）：**

每条的 model_name、thinking_enabled、thinking_effort、max_tokens、temperature、timeout_seconds 从当前 config.py 中的对应值迁移。

**search_platforms 预置（2条）：**

| provider_code | is_enabled | priority | max_results |
|--------------|------------|----------|-------------|
| ali1688 | 1 | 1 | 50 |
| madeinchina | 1 | 2 | 50 |

---

## 六、导航栏与路由权限改造

### 6.1 导航栏改造

在 `templates/base.html` 的导航栏中：

- **右侧**：显示当前登录用户的 display_name，下拉菜单包含"退出登录"
- **左侧菜单**：保持不变（首页、需求管理、供应商管理、AI功能）
- **管理员额外菜单**：仅当 `g.current_user.role == 'admin'` 时，导航栏多出一个"管理中心"入口

### 6.2 所有路由加上装饰器

```python
# 示例
@app.route("/")
@login_required
def index():
    ...

@app.route("/admin")
@admin_required
def admin_index():
    ...

@app.route("/admin/users")
@admin_required
def admin_users():
    ...
```

---

## 七、前端页面清单

### 新增模板文件

| 文件 | 路径 | 说明 |
|------|------|------|
| login.html | templates/login.html | 登录页（用户名+密码+注册链接） |
| register.html | templates/register.html | 注册页（用户名+显示名+密码+确认密码） |
| admin/index.html | templates/admin/index.html | 管理中心首页（快捷入口卡片） |
| admin/users/list.html | templates/admin/users/list.html | 用户列表 |
| admin/users/form.html | templates/admin/users/form.html | 新增/编辑用户表单 |
| admin/models/providers.html | templates/admin/models/providers.html | AI供应商列表 |
| admin/models/provider_form.html | templates/admin/models/provider_form.html | 新增/编辑供应商 |
| admin/models/configs.html | templates/admin/models/configs.html | 某供应商下的场景配置列表 |
| admin/models/config_form.html | templates/admin/models/config_form.html | 编辑单个场景配置 |

### 改造现有模板

- `templates/base.html`：导航栏加管理员入口、右上角加用户信息+退出
- `templates/index.html`：仪表盘数据加用户隔离

---

## 八、配置文件改造

### 8.1 config.py 的变化

改造后 `config.py` 将大幅简化，只保留：

- 数据库连接信息（如果有）
- `DB_PATH` 等非敏感的路径配置
- `load_config_from_db()` 等函数：从数据库加载AI模型配置、搜索平台配置到内存

原有的 API Key、模型名、思考强度等全部删除，改为从数据库读取。

**特别注意**：`config.py` 中曾用于联网搜索（web_search）的配置项（如 `ZHIPU_API_KEY` 等）仍然保留，因为智谱API Key还需用于图片识别（vision_ocr场景），只是不再用于联网搜索。

### 8.2 从 config.py 迁移到数据库的配置项清单

| 原config.py变量 | 迁移到 |
|----------------|--------|
| `DEEPSEEK_API_KEY` | ai_providers.api_key (provider_code=deepseek) |
| `DEEPSEEK_BASE_URL` | ai_providers.base_url (provider_code=deepseek) |
| `DEEPSEEK_MODEL` | ai_model_configs.model_name (每条场景各自配置) |
| `DEEPSEEK_THINKING_ENABLED` | ai_model_configs.thinking_enabled (每条场景各自配置) |
| `DEEPSEEK_THINKING_EFFORT` | ai_model_configs.thinking_effort (每条场景各自配置) |
| `DEEPSEEK_EFFORT_SIMPLE` | 废弃（每条场景独立配置，不再分简单/复杂两档） |
| `DEEPSEEK_EFFORT_COMPLEX` | 废弃（同上） |
| `DEEPSEEK_MAX_TOKENS` | ai_model_configs.max_tokens (每条场景各自配置) |
| `DEEPSEEK_TIMEOUT` | ai_model_configs.timeout_seconds (每条场景各自配置) |
| `ZHIPU_API_KEY` | ai_providers.api_key (provider_code=zhipu) |
| `ZHIPU_BASE_URL` | ai_providers.base_url (provider_code=zhipu) |
| `ZHIPU_VISION_MODEL` | ai_model_configs.model_name (scene_code=vision_ocr) |
| `TYC_MCP_URL` | ai_providers.base_url (provider_code=tianyancha) |
| `TYC_MCP_AUTH` | ai_providers.api_key (provider_code=tianyancha) |
| `ALI_1688_AK` | ai_providers.api_key (provider_code=ali1688) |

---

## 九、文件变更清单汇总

### 新增文件（约15个）

| 文件 | 说明 |
|------|------|
| `model_config.py` | 模型配置加载模块（从DB读配置到内存全局字典，含search_platforms） |
| `templates/login.html` | 登录页 |
| `templates/register.html` | 注册页 |
| `templates/admin/index.html` | 管理中心首页（用户管理+模型与平台管理入口卡片） |
| `templates/admin/users/list.html` | 用户列表 |
| `templates/admin/users/form.html` | 用户表单（新增/编辑） |
| `templates/admin/models/index.html` | 模型与平台管理首页（Tab切换：AI模型/搜索平台） |
| `templates/admin/models/providers.html` | AI供应商列表（Tab1一级） |
| `templates/admin/models/provider_form.html` | AI供应商编辑表单 |
| `templates/admin/models/configs.html` | 场景配置列表（Tab1二级） |
| `templates/admin/models/config_form.html` | 场景配置编辑表单 |
| `templates/admin/platforms/list.html` | 搜索平台列表（Tab2） |
| `templates/admin/platforms/form.html` | 搜索平台编辑表单 |

### 改造文件（约6个）

| 文件 | 改造内容 |
|------|---------|
| `app.py` | 加login_required/admin_required装饰器；加用户/管理员/模型管理/平台管理路由；加before_request中获取当前用户；所有查询加user_id过滤 |
| `db.py` | 新增users/ai_providers/ai_model_configs/search_platforms四张表；ai_providers加provider_type字段；现有四张表加user_id列 |
| `config.py` | 大幅精简，所有AI配置删除；保留路径等非敏感配置；新增从数据库加载配置的函数 |
| `ai_helper.py` | call_deepseek改为从内存配置读取参数，增加scene_code参数；call_zhipu_vision同样改造 |
| `supplier_search.py` | 所有call_deepseek调用加上scene_code；删除zhipu_web_search及所有联网搜索相关代码；天眼查/1688/中国制造网MCP改为从内存配置读取；搜索流程改为从g_search_platforms读取要用的平台 |
| `templates/base.html` | 导航栏加管理员入口+用户信息+退出

### 新增依赖

在 `requirements.txt` 中增加：

```
bcrypt==4.2.1       # 密码哈希
```

---

## 十、实施步骤建议

按以下顺序逐步开发，每步完成后可以独立测试：

| 步骤 | 内容 | 预计影响 |
|------|------|---------|
| **第1步** | `db.py` 新增四张表 + 现有表加user_id列 | 仅数据库，不影响运行 |
| **第2步** | 新建 `model_config.py`：从DB加载所有配置到内存（含模型配置+搜索平台配置） | 仅新增，不影响运行 |
| **第3步** | `app.py` 加登录/注册路由 + login_required装饰器 | 核心改动，从此需要登录 |
| **第4步** | `templates/` 新建登录页和注册页 | 前端页面 |
| **第5步** | `app.py` 所有查询加user_id过滤 | 数据隔离生效 |
| **第6步** | `templates/base.html` 导航栏改造 | 显示用户信息+管理中心入口 |
| **第7步** | `supplier_search.py` 删除zhipu_web_search及联网搜索调用 | 清理废弃功能 |
| **第8步** | `ai_helper.py` 改造call_deepseek/call_zhipu_vision（加scene_code） | AI调用改为读内存配置 |
| **第9步** | `supplier_search.py` 改造所有AI调用点+搜索平台动态选择 | 同上 |
| **第10步** | 管理中心-用户管理（路由+页面） | 管理员功能 |
| **第11步** | 管理中心-模型管理Tab1（AI模型配置） | 核心功能 |
| **第12步** | 管理中心-模型管理Tab2（搜索平台管理） | 平台选择功能 |
| **第13步** | `config.py` 清理 + 预置数据迁移脚本 | 收尾 |
| **第14步** | 全流程测试 | 回归测试 |

---

## 十一、关键注意事项

1. **向后兼容**：改造期间保持SQLite不变，MySQL迁移可以作为后续独立任务。

2. **首次启动**：系统需要检测 `users` 表是否为空，为空则自动创建admin账号；检测 `ai_providers` 是否为空，为空则从 config.py 迁移数据。这个逻辑放在 `db.init_db()` 或 `app.py` 的启动逻辑中。

3. **配置热更新**：管理员修改模型配置后，除了更新数据库，还要立即刷新内存中的 `g_model_configs` 字典。需要加一个全局锁（`threading.Lock`）保护读写，避免SSE搜索线程正在读配置时被刷新覆盖。

4. **密码安全**：使用 bcrypt 哈希存储密码，永不明文存储。登录验证时用 `bcrypt.checkpw()` 比对。

5. **session 安全**：`app.secret_key` 务必改为随机字符串（当前是写死的 "sourcing-system-secret-key"）。可以用 `os.urandom(24).hex()` 生成。

6. **API Key 展示**：在模型管理页面展示 API Key 时，默认显示为 `sk-****...****b86`（脱敏展示），点击"显示"才展示完整Key。防屏幕窥视。

7. **场景代码不可修改**：`scene_code` 是代码中写死的标识，管理员不能在页面修改它。只能修改该场景对应的模型名、参数等。

# 项目说明书 —— 供应商寻源系统

> 最后更新：2026-08-28
> 接手必读：本文档是本项目的 PRD（产品需求文档），记录项目目的、实现方案与关键进度。换人接手时先读本文即可快速了解全貌。

---

## 一、项目目的

为采购/业务人员提供一套**从"采购需求"到"合格供应商名单"的全流程自动化工具**：

1. 用户输入需求（文字 / 文档 / 图片 / 电商商品链接）
2. AI 自动解析为结构化需求（产品名、材质、规格、市场、认证等），缺失项自动追问
3. 需求确认后，自动在 1688、中国制造网（MIC）等 B2B 平台搜索产品并获取供应商
4. 自动补全供应商工商信息（注册资本、联系方式等），无有效联系方式直接否决
5. AI + 规则双重筛选，最终输出合格供应商名单，支持邮件/短信沟通

**核心原则（硬约束）**：
- 不用 AI 推荐供应商，只搜真实平台数据；供应商名称必须严格匹配，不做模糊放行
- 数据源全部走官方 API / MCP 服务，不做网页爬虫兜底
- 展示页面面向业务人员，无技术术语

---

## 二、项目实现方案

### 2.1 技术栈

| 层 | 技术 |
|----|------|
| Web 框架 | Python Flask（app.py），Jinja2 模板 + Bootstrap |
| 数据库 | SQLite（db.py） |
| 主力大模型 | MiniMax-M3（OpenAI 兼容接口，API Key 读环境变量 `minimax` / `MINIMAX_API_KEY`） |
| 图片识别 | MiniMax-M3 视觉（主）+ 智谱 GLM-4V（自动回退） |
| 外部数据源 | 1688 开放平台 API（skills-gateway，`/api/find_product/1.0.0`）、中国制造网 MCP、天眼查 MCP、水滴 MCP、快查365 MCP |

### 2.2 核心模块（代码地图）

| 文件 | 职责 |
|------|------|
| app.py | Flask 路由、页面逻辑、会话登录 |
| ai_helper.py | AI 能力：需求解析、追问生成、总结与 P0-P3 关键词、视觉识别、LLM 调用封装 |
| supplier_search.py | 供应商搜索编排：1688 + MIC 多平台搜索、名称正则提取、联系方式校验 |
| shuidi_client.py | 水滴 MCP 客户端（工商信息补全、注册资本单位归一化为"万"） |
| kuaicha_client.py | 快查365 MCP 客户端（英文供应商首选/中文降级，一次返回工商+电话/邮箱/官网） |
| screening_engine.py / screening_rules.py / screening_data.py | 供应商筛选（规则+AI 合一，疑似制造商保留标记） |
| communication_ai.py / email_service.py | 与供应商的邮件/短信沟通内容生成与发送 |
| file_parser.py | 上传文档文本提取 |
| scoring.py | 产品匹配打分（核心功能/材质对齐优先） |
| model_config.py | 模型供应商/场景温度等配置（管理后台可调） |
| templates/ | 前端页面（业务向，无技术注释） |

### 2.3 关键业务流程

```
需求输入（文字/文档/图片/链接）
   → 网页抓取（Jina/Firecrawl 多级兜底）+ 图片AI识别
   → MiniMax-M3 结构化提取（字段齐全→confirmed）
        ├─ 未齐全 → 生成中文追问 → 用户补充 → 再解析
        └─ 齐全 → 生成需求总结 + P0-P3 关键词（固定7组，每组中文/英文/15个变体）
   → 供应商搜索：1688 API（原关键词、pageSize 数据库可配、全局5秒间隔）
                 + MIC MCP（search_products 直连、3重试6秒间隔）
   → 名称正则提取 → MiniMax-M3 过滤（结果必须与原列表严格匹配）
   → 工商补全（按语言优先级链，命中即停，停用服务商自动剔除）：
        中文：水滴 → 快查365 → 天眼查；英文：快查365 → 天眼查
   → 无有效电话+邮箱 → 直接否决；MIC来源全链查不到 → 剔除
   → AI+规则筛选 → 合格供应商名单 → 邮件/短信沟通
```

### 2.4 语言与品牌规范（2026-08-28 新增）

AI 解析需求的输出**全部中文化**，且**剔除他人品牌**：
- 所有提取字段（产品名、核心功能、材质、规格、市场等）必须中文；英文网页/文档内容提取后**翻译成中文**再输出
- 例外可保留英文：产品型号、认证缩写（CE/FCC/RoHS/UL）、单位符号（mm/kg/mAh）
- `requirement_summary` 与关键词的 `cn`/`variants` 必须中文；仅关键词 `en` 字段用英文（供海外平台搜索）
- **品牌剔除**：用户贴的链接只是参考产品，售卖的是别人的品牌；品牌名（Brand字段、标题品牌词等）
  一律不提取、不展示，需求总结与 P0-P3 关键词也不含品牌词——用户采购"同款产品"，不买品牌

---

## 三、项目进度

| 日期 | 事项 | 状态 |
|------|------|------|
| （早期） | 基础框架：Flask + 数据库 + 需求/供应商管理页面 | ✅ 完成 |
| （早期） | 1688 API 接入（find_product，全局5秒限流、快速重试） | ✅ 完成 |
| （早期） | MIC MCP 接入（search_products 直连、串行+重试防限流） | ✅ 完成 |
| （早期） | MiniMax-M3 视觉识别 + GLM-4V 回退（实测床品图识别准确） | ✅ 完成 |
| （早期） | 水滴/天眼查工商补全、注册资本归一化"万"、联系方式否决规则 | ✅ 完成 |
| （早期） | AI筛选与严格筛选合并，疑似制造商保留 | ✅ 完成 |
| （早期） | 邮件/短信沟通、管理后台（模型/平台/用户/模板配置） | ✅ 完成 |
| 2026-08-28 | AI 解析输出全面中文化（英文内容翻译后输出，关键词 en 除外） | ✅ 完成 |
| 2026-08-28 | 品牌剔除：链接参考商品的他人品牌不提取、不展示（产品名/别名/总结/关键词全链路生效） | ✅ 完成 |
| 2026-09-11 | 服务商管理：编辑页"服务商说明"新增控制台入口（DeepSeek/智谱/MiniMax/1688/海关/天眼查/快查365/水滴，点击直达换Key页面） | ✅ 完成 |
| 2026-09-11 | 接入快查365 MCP（同花顺企业数据引擎），企业信息查询改为优先级链：中文=水滴→快查→天眼查，英文=快查→天眼查；停用服务商自动剔除；英文供应商实测命中并返回电话/邮箱/官网 | ✅ 完成 |

### 待办事项

- [ ] （无紧急待办）如需验证：用一条亚马逊英文品牌商品链接跑需求解析，确认字段均为中文且不含品牌名

### 方案变更记录

- 2026-08-28：新增"语言规范"——AI 解析需求输出强制中文（见 2.4 节），仅关键词英文字段保留英文。
- 2026-08-28：语言规范扩充为"语言与品牌规范"——参考链接的他人品牌在提取、总结、关键词环节全部剔除。
- 2026-09-11：企业信息查询数据源改造为优先级链（见 2.3 节）——中文：水滴→快查→天眼查；英文：快查→天眼查。快查365（provider_code=kuaicha_data）替代天眼查成为英文链路首选，并作为中文链路第二顺位；服务商在后台停用即自动剔除，不再参与查询。

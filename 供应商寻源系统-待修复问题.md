# 供应商寻源系统 - 待修复逻辑问题

> 本文档汇总了当前系统在架构和逻辑层面存在的不合理之处，均为尚未修改的问题。每个问题包含问题描述、涉及文件与行号、当前代码片段、以及建议的修复方向。

---

## 1. 流程顺序颠倒：DeepSeek 过滤在工商数据补全之前

### 严重程度：核心架构问题

### 当前流程（错误）

```
搜索(1688+MCP) → DeepSeek过滤 → 天眼查工商补全 → 写入数据库
                         ↑
                    做判断时看不到注册资本/经营状态/成立年限
                    一个注册资本1万的皮包公司和5000万的实体工厂看不出区别
```

### 建议流程

```
搜索(1688+MCP) → 程序化预筛 → 天眼查工商补全 → DeepSeek基于完整数据过滤 → 写入数据库
```

### 涉及文件

`supplier_search.py` 的 `search_suppliers()` 函数（约1586行起），需要把当前"先过滤后补全"的顺序反过来。当前 `total_steps = 3`，改动后应为 `total_steps = 4`：

| 步骤 | 内容 |
|:---:|------|
| 1/4 | 并发搜索所有关键词 |
| 2/4 | 程序化预筛（正则+平台字段，不调AI，零token成本，约100~150家） |
| 3/4 | 天眼查批量补全工商信息 |
| 4/4 | DeepSeek基于完整数据做精细过滤 |

DeepSeek 过滤时（`_filter_one_batch`，约1388行）的 prompt 中 `companies_text` 应追加：
- 注册资本（从步骤3获取）
- 经营状态（从步骤3获取）
- 成立年限（从步骤3获取）
- 注册地址（从步骤3获取）

---

## 2. 天眼查匹配失败时取第一个结果，会污染数据

### 严重程度：数据准确性问题

### 问题描述

当前代码在精确公司名匹配失败时，直接取天眼查返回列表的第一条结果作为匹配。

### 涉及文件与行号

`supplier_search.py`，约1758-1765行

### 当前代码

```python
companies = tyc_client.search_companies(name)
matched_company = None
for company in companies:
    if company.get("name", "").strip() == name:
        matched_company = company
        break
if not matched_company and companies:
    matched_company = companies[0]   # <-- 危险：取了一个可能完全不相关的公司
```

### 问题场景

搜"深圳鑫辉电子有限公司"，天眼查没找到精确匹配，返回列表第一是"深圳鑫辉贸易有限公司"。结果这家贸易公司的注册资本、电话、地址被错误写到了电子公司身上。

### 修复方向

- 精确匹配失败时不走 fallback，将该供应商标记为"工商数据未匹配"
- 或至少做公司名相似度计算（编辑距离/关键词交集），低于阈值不采用
- 绝对不能取 `companies[0]` 作为兜底

---

## 3. intro 字段被天眼查无条件覆盖

### 严重程度：数据质量问题

### 问题描述

DeepSeek 在过滤阶段生成了面向采购决策的 intro（如"深圳市XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），但在天眼查补全阶段被工商简介无条件覆盖（如"经营范围：电子产品、通讯设备、计算机软硬件…"），丢了采购决策视角的关键信息。

### 涉及文件与行号

`supplier_search.py`，约1775-1777行

### 当前代码

```python
tyc_intro = detail.get("intro", "")
if tyc_intro:
    supplier["intro"] = tyc_intro   # 无条件覆盖 DeepSeek 生成的 intro
```

### 修复方向

- 天眼查的工商简介不覆盖 DeepSeek 的 intro，而是存为独立字段（如 `business_profile` 新增数据库列）
- 或至少将天眼查简介追加到 DeepSeek intro 后面，而非覆盖
- DeepSeek 的 intro 保留面向采购的视角，天眼查 profile 作为补充工商信息

---

## 4. 跨平台（1688/MCP）无去重

### 严重程度：数据重复 + 资源浪费

### 问题描述

1688 API 和 MCP 是独立搜索的，同一家工厂可能同时在1688和Made-in-China上有账号。入库后变成两条独立的供应商记录，导致：

- DeepSeek 要多分析一遍（浪费 token）
- 天眼查要多查一遍（浪费 API 调用）
- 用户看到两条重复条目（体验差）

更麻烦的是，MCP 的公司名经 DeepSeek 批量翻译后格式变成了"深圳市XX科技有限公司（Shenzhen XX Technology Co., Ltd.）"，和1688返回的纯中文名"深圳市XX科技有限公司"无法直接字符串匹配。

### 涉及文件与行号

`supplier_search.py`，`search_suppliers()` 中合并搜索结果后（约1695行），以及 `crawl_1688`（约902行）和 `crawl_made_in_china`（约557行）的各自去重逻辑仅限单平台内。

### 修复方向

- 在 `extract_company_names` 或步骤2中新增跨平台去重
- 将 MCP 翻译后的公司名去掉括号中的英文部分，再与1688名称做模糊匹配
- 去重策略：先提取括号前的中文名 → 去除"有限公司/有限责任公司/股份公司"后缀 → 比较字号（公司核心名） → 相同则保留数据更丰富的那条

---

## 5. DeepSeek 过滤批次失败静默丢失

### 严重程度：数据丢失

### 问题描述

`_filter_one_batch` 函数在执行异常时（JSON解析失败、API超时、网络错误等）直接返回空数组 `[]`，该批次约50家公司全部丢失，不报错、不重试、不 fallback。

### 涉及文件与行号

`supplier_search.py`，约1493-1495行（函数末尾）

### 当前代码

```python
    except Exception as e:
        print(f"批次过滤失败: {e}")
        return []     # <-- 50家公司直接丢失
```

### 修复方向

- 异常时返回该批次的原始公司数据（标记为"过滤失败-保留"），而非丢弃
- 或增加重试机制（至少重试1次）
- 至少写入详细日志记录丢失了哪些公司
- filter_suppliers_with_ai 中也需要感知哪个批次失败了（约1519行起）

---

## 6. 1688 返回数据缺乏产品信息

### 严重程度：判断依据不足

### 问题描述

1688 API 返回的供应商描述信息仅包含工厂能力数据：

```
当前1688 source_text示例：
"合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120；满意度：98%；支持打样"
```

这里面完全没有该工厂具体生产什么产品的信息。DeepSeek 只能从"能做 OEM 贴牌 + 你搜的是电视柜"来推断其 `main_product`，猜错概率很高。

### 涉及文件与行号

`supplier_search.py`，约875-893行（`_crawl_1688_once` 中组装 content 的部分）

### 修复方向

- 1688 API 返回的 `extInfos` 中可能有 `mainProducts` 或 `scope` 等字段，检查完整返回数据是否遗漏
- 如果确实没有，在 DeepSeek prompt 中明确告知："1688平台数据不含主营产品描述，请根据公司名+工厂能力+搜索关键词谨慎推断，不确定时标注'待确认'"

---

## 7. source_text 截断 200 字符可能丢失关键信息

### 严重程度：判断依据不足

### 问题描述

MCP 返回的 content 包含"业务类型 + 主营产品列表 + 地区 + 认证列表"，信息密度高。硬截断 200 字符可能刚好把认证（ISO/BSCI等）或后半段主营产品截掉。

### 涉及文件与行号

`supplier_search.py`，约1343行、1370行

### 当前代码

```python
"source_text": r.get("content", "")[:200],    # 硬截断
```

### 修复方向

- 改为智能截断：按分号切句 → 保留完整的句子直到接近长度上限
- 或将截断上限提高到 400~500 字符
- 或不在 extract 阶段截断，改为传入完整数据给 DeepSeek 后由模型自行处理

---

## 附录：已修复的问题（供参考，无需再改）

| 问题 | 状态 |
|------|:---:|
| product_title / product_link / price_moq 三个无用字段仍在管道中传递和写入数据库 | 已清理 |
| P3 关键词生成示例中"储物柜"替代了核心品类"电视柜" | 已修复 prompt |
| P3_2 为凑数而用"客厅柜"等偷换概念的词 | 已修复 prompt（允许 P3_1=P3_2） |
| DeepSeek 思考模式 max 强度 + max_tokens=32768 导致 token 消耗过高 | 已在 config.py 注释中说明调参方法 |
| 问题1：流程顺序颠倒（DeepSeek过滤在工商补全之前） | 已修复：改为搜索→预筛→天眼查补全→DeepSeek过滤（4步流程） |
| 问题2：天眼查匹配失败取第一个结果 | 已修复：改为相似度校验（>=0.6才采用），未匹配标记"工商数据未匹配" |
| 问题3：intro被天眼查无条件覆盖 | 已修复：改为追加"工商经营范围："（用户选追加方案） |
| 问题4：跨平台无去重 | 已修复：提取中文核心字号去重，保留信息更丰富的那条 |
| 问题5：DeepSeek过滤批次失败静默丢失 | 已修复：失败重试1次，仍失败则保留原始数据标记"过滤失败-保留" |
| 问题6：1688返回数据缺乏产品信息 | 已修复：content加入搜索品类+尝试提取extInfos产品字段，prompt增加1688数据特点说明 |
| 问题7：source_text截断200字符 | 已修复：改为按分号切句智能截断，上限400字符 |

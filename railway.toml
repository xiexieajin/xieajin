[build]
# Railpack 自动检测Python项目并构建

[deploy]
# 明确启动命令，确保 Railway 用正确的端口
startCommand = "python app.py"

# 健康检查配置 - Railway 每隔一定时间访问这个路径判断应用是否存活
healthcheckPath = "/health"
# 容器启动后等待10秒才开始健康检查（给 waitress 足够的启动时间）
healthcheckTimeout = 30

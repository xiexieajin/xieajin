# 供应商寻源# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

|# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names`# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch`# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq`# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"180# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

### 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supp# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
## 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fall# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform`# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 168# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1.# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 business# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_c# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM,# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `main# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2.# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3.# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

#### 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fall# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

#### 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guang# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py`# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

#### 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSe# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 +# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 |# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

##### 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 |# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司）# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 |# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

#### 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile`# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand,# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand, Coffee Table"`
- 如果 DeepSeek 返回的 main_product 与 MCP mainProducts# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand, Coffee Table"`
- 如果 DeepSeek 返回的 main_product 与 MCP mainProducts 明显矛盾，优先采信 MCP# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand, Coffee Table"`
- 如果 DeepSeek 返回的 main_product 与 MCP mainProducts 明显矛盾，优先采信 MCP 数据

---

## 六、🐌 DeepSeek Token 消耗问题（参考）

**根# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand, Coffee Table"`
- 如果 DeepSeek 返回的 main_product 与 MCP mainProducts 明显矛盾，优先采信 MCP 数据

---

## 六、🐌 DeepSeek Token 消耗问题（参考）

**根因**：系统的 `config.py` 开启了 `reasoning_effort = "max"# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand, Coffee Table"`
- 如果 DeepSeek 返回的 main_product 与 MCP mainProducts 明显矛盾，优先采信 MCP 数据

---

## 六、🐌 DeepSeek Token 消耗问题（参考）

**根因**：系统的 `config.py` 开启了 `reasoning_effort = "max"` 的思考模式，且 `max_tokens = 32768`。供应商过滤是# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand, Coffee Table"`
- 如果 DeepSeek 返回的 main_product 与 MCP mainProducts 明显矛盾，优先采信 MCP 数据

---

## 六、🐌 DeepSeek Token 消耗问题（参考）

**根因**：系统的 `config.py` 开启了 `reasoning_effort = "max"` 的思考模式，且 `max_tokens = 32768`。供应商过滤是复杂任务，DeepSeek 在 `max# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand, Coffee Table"`
- 如果 DeepSeek 返回的 main_product 与 MCP mainProducts 明显矛盾，优先采信 MCP 数据

---

## 六、🐌 DeepSeek Token 消耗问题（参考）

**根因**：系统的 `config.py` 开启了 `reasoning_effort = "max"` 的思考模式，且 `max_tokens = 32768`。供应商过滤是复杂任务，DeepSeek 在 `max` 强度下会产生大量内部推理链（reasoning_content），按 output token 计费# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand, Coffee Table"`
- 如果 DeepSeek 返回的 main_product 与 MCP mainProducts 明显矛盾，优先采信 MCP 数据

---

## 六、🐌 DeepSeek Token 消耗问题（参考）

**根因**：系统的 `config.py` 开启了 `reasoning_effort = "max"` 的思考模式，且 `max_tokens = 32768`。供应商过滤是复杂任务，DeepSeek 在 `max` 强度下会产生大量内部推理链（reasoning_content），按 output token 计费（6元/百万tokens）。

**为什么 Claude Code 里同样配置消耗少得多**# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand, Coffee Table"`
- 如果 DeepSeek 返回的 main_product 与 MCP mainProducts 明显矛盾，优先采信 MCP 数据

---

## 六、🐌 DeepSeek Token 消耗问题（参考）

**根因**：系统的 `config.py` 开启了 `reasoning_effort = "max"` 的思考模式，且 `max_tokens = 32768`。供应商过滤是复杂任务，DeepSeek 在 `max` 强度下会产生大量内部推理链（reasoning_content），按 output token 计费（6元/百万tokens）。

**为什么 Claude Code 里同样配置消耗少得多**：Claude Code 里的任务是简短回答和# 供应商寻源系统 — 问题汇总与改建建议

> 整理自 2026-07-15 系统审计对话。
> 已完成的改动标注"✅已改"，未开始的标注"🔴待改 / 🟡建议改 / 📋参考"。

---

## 一、✅ 已完成改动

### 1.1 废弃字段清理

`product_title`、`product_link`、`price_moq` 三个字段在简化 1688 和中国制造网的搜索逻辑后已不再获取（1688 改用直接搜供应商的接口、MCP 不再返回产品详情），但代码管道中仍带着空值流转。已做以下清理：

| 文件 | 改动 |
|------|------|
| `supplier_search.py` | MCP 搜索结果组装中去掉 `title`/`link`/`price_moq` 三个空字段 |
| `supplier_search.py` | `extract_company_names` 不再读写这三个字段 |
| `supplier_search.py` | `_filter_one_batch` 不再合并这三个字段回过滤结果 |
| `supplier_search.py` | DeepSeek prompt 中去掉 `price_moq` 的输出要求 |
| `app.py` | SQL INSERT 不再写入这三个列 |
| `db.py` | 新增 `_drop_column_if_exists` 删除旧表中的三列 |

### 1.2 关键词生成 Prompt 优化

问题：ID9 需求（"1800mm木质茶色玻璃台面带插座灯带储物电视柜"），P3 关键词生成了"储物柜"，把"带储物功能的电视柜"错判为另一个品类"储物柜"。P0 的示例中也出现了"客厅柜"偷换品类词的情况。

修改位置：`ai_helper.py` 的 `_generate_summary_and_keywords` 函数。

新增规则：
- 产品的功能/特征/附属物可修饰品类词，但绝不能替代品类词独立成为关键词
- P3 只保留核心品类词本身，如果找不到第二个合理品类词，P3_2 可以和 P3_1 相同
- 正反示例覆盖了电视柜、智能手机、蓝牙音箱三种不同类型产品

---

## 二、🔴 架构问题（建议尽快改）

### 2.1 天眼查匹配失败时 fallback 到第一个结果，会污染数据

**位置**：`supplier_search.py` 第 1760-1765 行

```python
# 当前逻辑
if not matched_company and companies:
    matched_company = companies[0]  # ❌ 危险！
```

**问题**：当"深圳鑫辉电子有限公司"在天眼查中找不到同名匹配时，fallback 取了搜索结果的第一条"深圳鑫辉贸易有限公司"——完全不同的公司，但它的注册资本、电话、地址被错误地写入了正确供应商身上。

**改建**：去掉这个 fallback，找不到同名匹配时只标记"天眼查未匹配"，不写入任何工商数据。防止脏数据比"少一条记录"更重要。

---

### 2.2 跨平台（1688 vs 中国制造网）完全没有去重

**位置**：`supplier_search.py` 的 `_search_one_keyword` 函数（第 1673-1694 行）

**问题**：同一家工厂可能在 1688 和中国制造网都有账号。当前两个平台独立搜索、独立存库（`source_platform` 不同），导致：
- 同一家供应商在数据库里有两条记录
- DeepSeek 要过滤两次、天眼查要查两次
- 用户看到重复条目

更大的困难在于：MCP 返回的英文公司名经过 DeepSeek 翻译后格式为"中文名（英文原名）"，而 1688 返回的是纯中文名，两者很难直接字符串匹配。

**改建**：
1. 入库前基于公司名称相似度和地区做跨平台去重
2. 或者翻译后将括号里的英文原名去掉，提取纯中文名再和 1688 的公司名做模糊匹配
3. 去重后保留信息更全的一条（MCP 有 businessType/badges，1688 有合作方式/工厂等级），合并为一条

---

### 2.3 1688 返回的数据完全没有产品信息

**位置**：`supplier_search.py` 的 `_crawl_1688_once` 函数（第 875-893 行）

**问题**：1688 返回的 `content` 格式是：

```
合作方式：OEM, ODM；服务：贴牌加工, 来样加工；地区：广东深圳；工厂等级：A；月订单：120
```

这里面**没有一行**描述这家工厂生产什么产品。DeepSeek 只能从"这家能力匹配 + 你搜的关键词里有电视柜"来**猜**它的 `main_product`。

相比之下，MCP 的 `mainProducts` 是供应商自己填的（如 "TV Stand, Coffee Table, Storage Cabinet"），可靠性高得多。

**改建**：
1. 将 MCP 的 `mainProducts` 在传给 DeepSeek 的 source_text 中置顶标注
2. 1688 这边如果 API 有返回产品相关字段（如 `mainProducts` 或商品标题），补上获取逻辑
3. DeepSeek prompt 中提示优先参考 MCP 的 mainProducts 字段

---

### 2.4 DeepSeek 过滤某批次失败时，那批公司静默丢失

**位置**：`supplier_search.py` 的 `_filter_one_batch` 函数（第 1481-1482 行）

```python
except Exception as e:
    print(f"批次过滤失败: {e}")
    return []  # ❌ 静默丢弃
```

**问题**：如果某批 50 家公司因为 JSON 解析失败或超时而异常，这批公司直接丢弃，不报错、不重试、不 fallback。整个流程不感知，用户只知道"搜出来这么少"，无法追溯原因。

**改建**：
1. 失败时至少把这批公司的原始 name 保底输出（不做过滤判断，标记 source 为"AI过滤失败"）
2. 在进度回调中报告失败批次和原因
3. 加一次重试机制

---

## 三、🟡 可容忍但建议改

### 3.1 source_text 截断 200 字符过于粗暴

**位置**：`supplier_search.py` 的 `extract_company_names`（第 1343 行）

```python
"source_text": r.get("content", "")[:200]  # 硬截断
```

**问题**：MCP 的 content 可能包含"业务类型：Manufacturer；主营：TV Stand, Coffee Table, Entertainment Center；地区：Guangdong；认证：ISO9001, BSCI"。200 字符可能刚好把认证列表截断。

**改建**：改为 `[:500]`，或按最后完整的分号分隔取整句。

---

### 3.2 天眼查两步查询耦合过紧

**位置**：`supplier_search.py` 的 `_enrich_one_supplier`（第 1750-1818 行）

**问题**：每家供应商都 `search_companies` → `get_company_basic_profile` 两次调用。但 `search_companies` 返回的 Markdown 表格已经包含注册资本、经营状态、法定代表人。`get_company_basic_profile` 只额外提供电话/邮箱/地址/intro。

**改建**：先跑 `search_companies`，如果手机号/邮箱/地址在前端不需要，就可以跳过第二步，大幅减少 API 调用。

---

## 四、🔁 核心流程重排（建议优先处理）

### 4.1 当前流程的问题

```
搜索 → DeepSeek过滤 → 天眼查补全 → 存库
         ↑                  ↑
    判断时看不到          补全后数据
    工商数据              再也没人用
```

DeepSeek 在做"是否制造商/是否完整产品/主营是否相关"的判断时，只能看到公司名和一段平台描述。注册资本、经营状态、成立年限这些关键信息 DeepSeek 完全不知道。

结果：注册资本 1 万的皮包公司和注册资本 5000 万的实体工厂，在 DeepSeek 眼里完全一样。

### 4.2 建议的三步流程

```
步骤A：快速预筛（程序判断，0 token 成本）
  → 用正则+平台字段过滤，把 200-400 家缩到 100 家左右
     ↓
步骤B：天眼查补全（仅对预筛后的 100 家）
  → 补全注册资本、经营状态、成立日期、地址
     ↓
步骤C：DeepSeek精细过滤（首次看到完整数据）
  → 输入：公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址
  → 输出：supplier_type、main_product、intro、has_cross_border_exp
```

#### 步骤A 的预筛规则（纯程序，0 token）

| 数据来源 | 筛掉条件 |
|---------|---------|
| MCP businessType | `Trading Company` 直接剔除 |
| 公司名正则匹配 | 含"贸易""商贸""电子商务""商行""经营部""零售"的剔除 |
| 公司名正则匹配 | 含"配件""材料""模具""板材""毛坯"等明显非成品的剔除 |
| 1688 数据 | oem_mode 或 manufacture_type 为空的剔除（已有） |
| 名称去重 | 按公司名去重（已有） |

#### 步骤C DeepSeek 基于工商数据的新判断维度

| 判断维度 | 来源 | 处理逻辑 |
|---------|------|---------|
| 经营状态 | 天眼查 | "注销/吊销/停业" → 直接淘汰 |
| 注册资本 | 天眼查 | < 10 万 → 扣分（可能皮包公司） |
| 成立年限 | 天眼查 | ≥ 5 年 → 加分 |
| 注册地址 | 天眼查 | 在知名产业带（佛山→家具、深圳→电子）→ 加分 |

### 4.3 前后对比

| 维度 | 当前流程 | 改进后 |
|------|---------|--------|
| DeepSeek 看到的输入 | 公司名 + "合作方式OEM" | 公司名 + 平台描述 + 注册资本 + 经营状态 + 成立年限 + 地址 |
| 天眼查调用量 | 50-100 家 | 约 100 家（预筛后） |
| 皮包公司过滤 | 无法识别 | 通过注册资本和经营状态识别 |
| 判断依据 | 主要靠猜测 | 有真实工商数据支撑 |

---

## 五、📋 字段获取逻辑优化

### 5.1 intro 字段双来源覆盖逻辑

**当前逻辑**：DeepSeek 生成 → 天眼查 profile **无条件覆盖**

**问题**：DeepSeek 生成的 intro 面向采购决策（"深圳XX电子有限公司，主营蓝牙音箱贴牌加工，支持OEM/ODM"），天眼查返回的是工商登记简介（"经营范围：电子产品、通讯设备、计算机软硬件的技术开发与销售..."）。两者目的不同，覆盖后丢失了有用的采购信息。

**改建**：
- 天眼查的 profile 单独存为 `business_profile` 字段（或附加到 intro 后面而非覆盖）
- 或者让 DeepSeek 基于 1688 content + MCP mainProducts + 天眼查 profile 综合生成一个面向采购决策的 intro

### 5.2 main_product 缺少真实数据源

**问题**：`main_product` 完全依赖 DeepSeek 推断。1688 的 source_text 不含产品信息，MCP 的 mainProducts 被压进 source_text 里没有优先标注。

**改建**：
- 在给 DeepSeek 的 source_text 中，将 MCP 的 `mainProducts` 置顶标注如 `"[平台数据] 供应商自填主营：TV Stand, Coffee Table"`
- 如果 DeepSeek 返回的 main_product 与 MCP mainProducts 明显矛盾，优先采信 MCP 数据

---

## 六、🐌 DeepSeek Token 消耗问题（参考）

**根因**：系统的 `config.py` 开启了 `reasoning_effort = "max"` 的思考模式，且 `max_tokens = 32768`。供应商过滤是复杂任务，DeepSeek 在 `max` 强度下会产生大量内部推理链（reasoning_content），按 output token 计费（6元/百万tokens）。

**为什么 Claude Code 里同样配置消耗少得多**：Claude Code 里的任务是简短回答和代码问答，prompt/output 几百 token；而这里是批量判断 50 家公司的资质